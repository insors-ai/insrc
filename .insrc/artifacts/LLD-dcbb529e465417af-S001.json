{
  "meta": {
    "workflow": "design.story",
    "runId": "wf-1785923420912-fydinv",
    "repoPath": "/Users/subhagho/work/projects/insors/insrc",
    "createdAt": "2026-08-05T09:59:26.109Z",
    "attribution": {
      "outputs": [
        {
          "role": "(client)",
          "tier": "core",
          "runner": "cli-claude",
          "model": "client"
        }
      ]
    },
    "elapsedMs": 545126,
    "repoIndexedAt": null,
    "schemaVersion": 1,
    "epicHash": "dcbb529e465417af",
    "epicSlug": "renamespace-def-local-constraint-ids-off",
    "storyId": "S001",
    "hldBaseRunId": "wf-1785923420912-fydinv",
    "hldEffectiveHash": "dcbb529e465417af4c5341c85c6b244c2f9c1aed596961c858dae750297198c1",
    "hldAmendmentsApplied": [],
    "standalone": true,
    "sizeClass": "small",
    "triageRationale": "Narrow going-forward-only contract change in src/workflow/runners/define: change the local-constraint id pattern off the c-namespace + align prompt guidance + tests. One obvious approach (mirror the immune k-constraint namespace); no caller fan-out; no migration.",
    "review": {
      "artifact": "design.story",
      "stage": "design.story",
      "verdict": "pass",
      "findings": [
        {
          "claimId": "cl1",
          "kind": "citation",
          "severity": "LOW",
          "premise": "The DEF Stories schema constrains localConstraints[].id to ^c\\d+$ (the field this LLD changes to ^lc\\d+$), keeps source at ^c\\d+$, and restricts acceptanceCriteria.operationalizes items to ^k\\d+$.",
          "evidence": "src/workflow/runners/define/schemas.ts carries the localConstraints block (localConstraints[].id ^c\\d+$ at :216, source ^c\\d+$ at :219 per direct read) and operationalizes items ^k\\d+$ at schemas.ts:204 (grep hit). The c-namespace is shared with citations (^c\\d+$ appears across orchestrator.ts citation schemas too). Confirmed.",
          "action": "None — the changed field + the k-only operationalizes are real.",
          "fixability": "auto"
        },
        {
          "claimId": "cl2",
          "kind": "citation",
          "severity": "LOW",
          "premise": "The synthesizer's citation guard CITATION_REF_RE matches only [[c(\\d+)]] (c-only) and validateCitations hard-fails a body ref not in the citations list; k/lc refs are ignored.",
          "evidence": "synthesizer.ts:85 CITATION_REF_RE = /\\[\\[c(\\d+)\\]\\]/g (c-only), :119 'no citation with that id exists' hard-fail, :93 validateCitations, and :202 validateCitations(renderedBody, artifact.citations) — the guard runs on the rendered body, so an lc-ref (non-c) is ignored exactly as claimed. Confirmed.",
          "action": "None — the guard behaviour is exactly as the LLD describes.",
          "fixability": "auto"
        },
        {
          "claimId": "cl3",
          "kind": "citation",
          "severity": "LOW",
          "premise": "design.story's readUpstream returns the whole story (incl. localConstraints) and multiple steps JSON.stringify(story) into their prompts, so local-constraint ids reach the LLD synthesizer.",
          "evidence": "design-story/index.ts:87 readUpstream + :169/:212 (and further) JSON.stringify(story) confirm the whole story (incl. localConstraints) is surfaced into the LLD prompts. Confirmed.",
          "action": "None — the leak path resolves.",
          "fixability": "auto"
        },
        {
          "claimId": "cl4",
          "kind": "citation",
          "severity": "LOW",
          "premise": "The DEF renderer emits each local constraint using c.id generically and checkConstraintCoverage builds localIds but only matches them against operationalizes (which is k-only), so its local-id branch never matches a c-id.",
          "evidence": "define.ts:278 checkConstraintCoverage, :282 localIds = localConstraints.map(c=>c.id), :285 matches operationalizes against globalIds ∪ localIds; since operationalizes is ^k\\d+$-only (schemas.ts:204) the localIds branch never matches a c-id — unaffected by the rename. The renderer (define.ts:170-176) uses c.id generically. Confirmed.",
          "action": "None — the preserved-consumer premise holds.",
          "fixability": "auto"
        },
        {
          "claimId": "cl5",
          "kind": "citation",
          "severity": "LOW",
          "premise": "The define runner's Stories prompt guidance (the prose this LLD aligns) instructs that operationalizes references Epic or the Story's localConstraints ids.",
          "evidence": "The define runner's Stories prompt guidance line 'operationalizes must reference constraint ids from Epic OR the Story's localConstraints' resolves at src/workflow/runners/define/index.ts:160 (direct read during grounding; the review grep was noisy on the plain word but the file + line are real). This is the prose the LLD aligns.",
          "action": "None — the prompt-guidance anchor resolves.",
          "fixability": "auto"
        }
      ],
      "counts": {
        "high": 0,
        "med": 0,
        "low": 5
      },
      "reviewedAt": "2026-08-05T10:00:41.459Z",
      "model": "client"
    },
    "approvedAt": "2026-08-05T10:36:23.056Z"
  },
  "body": {
    "hldContextSlice": {
      "frameworkSummary": "Standalone feature — no parent HLD. Design directly against the repo, grounded on the s1 analyze passes. There are no HLD shared contracts to honour.",
      "ownedContracts": [],
      "consumedContracts": [],
      "boundary": {
        "storyId": "S001",
        "owns": [],
        "depends": [],
        "internal": "Renamespace DEF local-constraint ids off the citation c-namespace"
      },
      "rolloutPhase": "standalone",
      "nonFunctional": {}
    },
    "contractDetails": {
      "surfaceLevel": "internal",
      "api": [],
      "dataModel": [
        {
          "entity": "DEF Stories schema — localConstraints[].id (JSON-Schema property)",
          "change": "field-modify",
          "details": "Narrow the local-constraint id pattern from the citation namespace ^c\\d+$ to a distinct ^lc\\d+$ so a local constraint id echoed into an LLD body renders as an lc-ref, which the synthesizer's c-only CITATION_REF_RE ignores (like a k-ref) instead of validating it as a dangling citation. This is the sole behavioural change. Enforced only at define time (new DEFs); existing on-disk DEFs are not re-validated/migrated (going-forward only).",
          "schemaDiff": "localConstraints[].id.pattern: '^c\\\\d+$' -> '^lc\\\\d+$'",
          "callSites": [
            "src/workflow/runners/define/schemas.ts"
          ]
        },
        {
          "entity": "DEF Stories schema — localConstraints[].source (JSON-Schema property)",
          "change": "invariant-change",
          "details": "DELIBERATELY UNCHANGED: source stays ^c\\d+$. A local constraint's source references a REAL DEF citation, so it must remain in the citation namespace; the DEF renderer emits it as a source citation and it grounds against the DEF's citation list. Documented here so the plan does not renamespace it (that was the rejected a3).",
          "schemaDiff": "localConstraints[].source.pattern: '^c\\\\d+$' (unchanged)",
          "callSites": [
            "src/workflow/runners/define/schemas.ts",
            "src/workflow/artifacts/define.ts"
          ]
        },
        {
          "entity": "define runner Stories prompt guidance (prose)",
          "change": "field-modify",
          "details": "Align the define runner's Stories prompt so the model MINTS local-constraint ids as lcN (and references them in the lc namespace) rather than cN, keeping the emitted JSON consistent with the tightened schema. Around the existing guidance line that describes local constraints / operationalizes; no schema-validated field references a c-local-constraint (operationalizes is ^k\\d+$-only), so this is a prose alignment, not a structural reference change.",
          "schemaDiff": "(prompt text: 'mint local-constraint ids as lcN, reference them in the lc namespace')",
          "callSites": [
            "src/workflow/runners/define/index.ts"
          ]
        },
        {
          "entity": "checkConstraintCoverage / DEF renderer (consumers) — contract preserved",
          "change": "invariant-change",
          "details": "NO CHANGE required. checkConstraintCoverage (define.ts:278-292) builds localIds from local-constraint ids but only matches them against acceptanceCriteria.operationalizes, which the schema restricts to ^k\\d+$ — so its localIds branch never matched a c-id and is unaffected by the c->lc rename. The DEF renderer (define.ts:170-176) uses c.id generically and renders lcN correctly. Recorded so the plan verifies (not edits) these consumers.",
          "callSites": [
            "src/workflow/artifacts/define.ts"
          ]
        }
      ],
      "interactionWithShared": []
    },
    "dataModelChanges": [
      {
        "entity": "DEF Stories schema — localConstraints[].id (JSON-Schema property)",
        "change": "field-modify",
        "details": "Narrow the local-constraint id pattern from the citation namespace ^c\\d+$ to a distinct ^lc\\d+$ so a local constraint id echoed into an LLD body renders as an lc-ref, which the synthesizer's c-only CITATION_REF_RE ignores (like a k-ref) instead of validating it as a dangling citation. This is the sole behavioural change. Enforced only at define time (new DEFs); existing on-disk DEFs are not re-validated/migrated (going-forward only).",
        "schemaDiff": "localConstraints[].id.pattern: '^c\\\\d+$' -> '^lc\\\\d+$'",
        "callSites": [
          "src/workflow/runners/define/schemas.ts"
        ]
      },
      {
        "entity": "DEF Stories schema — localConstraints[].source (JSON-Schema property)",
        "change": "invariant-change",
        "details": "DELIBERATELY UNCHANGED: source stays ^c\\d+$. A local constraint's source references a REAL DEF citation, so it must remain in the citation namespace; the DEF renderer emits it as a source citation and it grounds against the DEF's citation list. Documented here so the plan does not renamespace it (that was the rejected a3).",
        "schemaDiff": "localConstraints[].source.pattern: '^c\\\\d+$' (unchanged)",
        "callSites": [
          "src/workflow/runners/define/schemas.ts",
          "src/workflow/artifacts/define.ts"
        ]
      },
      {
        "entity": "define runner Stories prompt guidance (prose)",
        "change": "field-modify",
        "details": "Align the define runner's Stories prompt so the model MINTS local-constraint ids as lcN (and references them in the lc namespace) rather than cN, keeping the emitted JSON consistent with the tightened schema. Around the existing guidance line that describes local constraints / operationalizes; no schema-validated field references a c-local-constraint (operationalizes is ^k\\d+$-only), so this is a prose alignment, not a structural reference change.",
        "schemaDiff": "(prompt text: 'mint local-constraint ids as lcN, reference them in the lc namespace')",
        "callSites": [
          "src/workflow/runners/define/index.ts"
        ]
      },
      {
        "entity": "checkConstraintCoverage / DEF renderer (consumers) — contract preserved",
        "change": "invariant-change",
        "details": "NO CHANGE required. checkConstraintCoverage (define.ts:278-292) builds localIds from local-constraint ids but only matches them against acceptanceCriteria.operationalizes, which the schema restricts to ^k\\d+$ — so its localIds branch never matched a c-id and is unaffected by the c->lc rename. The DEF renderer (define.ts:170-176) uses c.id generically and renders lcN correctly. Recorded so the plan verifies (not edits) these consumers.",
        "callSites": [
          "src/workflow/artifacts/define.ts"
        ]
      }
    ],
    "interactionWithShared": [],
    "errorPaths": {
      "errorCases": [
        {
          "scenario": "A new define run emits a local constraint with a c-prefixed id (the old namespace) after the schema is tightened to ^lc\\d+$.",
          "detection": "The define runner's ajv structured-output validation checks localConstraints[].id against the new ^lc\\d+$ pattern and reports a pattern mismatch for that item.",
          "response": "The existing structured-output retry loop re-prompts the model (the aligned prompt guidance now instructs lcN ids), and a corrected object with lc-ids validates; no partial/invalid DEF is persisted.",
          "userImpact": "Transparent — a bounded retry; the approved DEF ends up with lc-namespaced local constraints.",
          "recoverable": true
        },
        {
          "scenario": "A local constraint's `source` references a citation id that does not exist in the DEF's citations list.",
          "detection": "Unchanged from today: `source` keeps its ^c\\d+$ shape and is validated by the DEF's own citation-grounding checks (checkConstraintCoverage covers operationalizes/assumption sources; the renderer emits the source citation validated against the DEF citation list).",
          "response": "Same behaviour as before this change — the rename does not touch source validation; a dangling source is caught exactly as it is today.",
          "userImpact": "None new — source grounding is preserved byte-for-byte.",
          "recoverable": true
        }
      ],
      "edgeCases": [
        {
          "input": "A Story with NO localConstraints (the common case).",
          "expected": "Nothing to renamespace; the schema change is inert and behaviour is byte-identical to today."
        },
        {
          "input": "An EXISTING on-disk DEF that already carries c-prefixed local constraints (e.g. DEF-761a43a6), later read by design.story.",
          "expected": "Not re-validated or migrated — the tightened pattern is enforced only at define-time for NEW DEFs. The legacy collision can still fire for such a DEF; this is the explicit going-forward-only boundary, not a regression introduced here."
        },
        {
          "input": "The LLD synthesizer echoes a NEW story's local constraint (an lc-namespaced ref) in the LLD body.",
          "expected": "The c-only CITATION_REF_RE does not match an lc-ref (exactly as it ignores a k-ref), so validateCitations does not treat it as a dangling citation — the LLD synthesizes successfully. This is the whole fix."
        },
        {
          "input": "The LLD synthesizer echoes a local constraint's `source` (a real c-citation) in the LLD body.",
          "expected": "The source c-ref still matches CITATION_REF_RE, but resolves numerically against the LLD's own citation list (LLDs number their citations from c1); unchanged, low-risk behaviour — the fix targets the constraint id, not source."
        }
      ],
      "invariantsToPreserve": [
        {
          "text": "The synthesizer citation guard is unchanged: CITATION_REF_RE stays c-only and validateCitations still hard-fails only dangling c-refs while tolerating unreferenced citations — lc-refs and k-refs remain ignored. This story renames the constraint namespace so it lands on the ignored side; it does NOT alter the guard.",
          "source": "c2"
        },
        {
          "text": "checkConstraintCoverage semantics are preserved: operationalizes stays ^k\\d+$-only and its localIds branch remains (harmlessly) unreachable for it, so the c->lc rename changes no coverage outcome.",
          "source": "c4"
        },
        {
          "text": "localConstraints[].source remains a citation reference in the ^c\\d+$ namespace, so the DEF-level grounding link from a local constraint to its citation is preserved (the rejected a3 would have broken this).",
          "source": "c1"
        }
      ]
    },
    "testStrategy": {
      "testLevels": [
        {
          "level": "unit",
          "purpose": "Prove the tightened DEF schema accepts the new lc-namespace for local-constraint ids and rejects the old c-namespace, while source stays a c-citation reference.",
          "subjects": [
            "a Story localConstraint with id 'lc1' validates against the define Stories schema",
            "a Story localConstraint with a c-namespaced id is REJECTED by the schema (pattern ^lc\\d+$ mismatch)",
            "a Story localConstraint with a c-namespaced source still validates (source pattern ^c\\d+$ unchanged)"
          ],
          "fixturesNeeded": [
            "the exported define Stories JSON schema + an ajv validate (the pattern that the define runner's structured-output validation uses)"
          ]
        },
        {
          "level": "unit",
          "purpose": "Prove the fix: the synthesizer's c-only citation guard ignores lc-namespaced refs, so an LLD body echoing a local constraint no longer fails as a dangling citation — while the guard still catches genuine dangling c-refs.",
          "subjects": [
            "validateCitations over a body containing an lc-ref (and a k-ref) with a citations list that does NOT include them returns ok (no dangling-citation failure)",
            "validateCitations over a body containing a c-ref absent from the citations list STILL fails (guard intact — not weakened)"
          ],
          "fixturesNeeded": [
            "validateCitations from src/workflow/synthesizer.ts + a minimal citations[] fixture"
          ]
        },
        {
          "level": "unit",
          "purpose": "Prove the diagnosed consumers are unaffected by the c->lc rename (contract preserved, not edited).",
          "subjects": [
            "checkConstraintCoverage over a Story with lc-namespaced localConstraints + k-only operationalizes yields NO coverage error",
            "the DEF renderer renders a local constraint whose id is 'lc1' with its source citation intact"
          ],
          "fixturesNeeded": [
            "a DefineBody fixture with one Story carrying an lc-local-constraint (a c-namespaced source) + a matching citation"
          ]
        }
      ],
      "acceptanceMapping": [
        {
          "criterionId": "ac1",
          "provingTests": [
            "a Story localConstraint with id 'lc1' validates against the define Stories schema",
            "a Story localConstraint with a c-namespaced id is REJECTED by the schema (pattern ^lc\\d+$ mismatch)",
            "a Story localConstraint with a c-namespaced source still validates (source pattern ^c\\d+$ unchanged)"
          ]
        },
        {
          "criterionId": "ac2",
          "provingTests": [
            "validateCitations over a body containing an lc-ref (and a k-ref) with a citations list that does NOT include them returns ok (no dangling-citation failure)",
            "validateCitations over a body containing a c-ref absent from the citations list STILL fails (guard intact — not weakened)"
          ]
        },
        {
          "criterionId": "ac3",
          "provingTests": [
            "checkConstraintCoverage over a Story with lc-namespaced localConstraints + k-only operationalizes yields NO coverage error",
            "the DEF renderer renders a local constraint whose id is 'lc1' with its source citation intact"
          ]
        }
      ],
      "testFramework": "node:test via npx tsx --test"
    },
    "migration": {
      "stateBefore": "The DEF Stories schema (src/workflow/runners/define/schemas.ts:216) constrains localConstraints[].id to ^c\\d+$ — the SAME namespace as citations[].id. When design.story surfaces a Story into LLD synthesis it JSON.stringify's the whole story (incl. localConstraints) into the prompt (design-story/index.ts:100 + 169/212/309/354/454/510), the LLD synthesizer echoes the constraints as c-refs, and the c-only guard (synthesizer.ts:85 CITATION_REF_RE, hard-fail at :119) rejects them as dangling citations. The define runner prompt (define/index.ts:160) describes local constraints in the c-namespace. k-constraints (^k\\d+$) are immune to the c-only guard.",
      "stateAfter": "localConstraints[].id is constrained to ^lc\\d+$ (schemas.ts:216) and the define runner prompt guidance mints local-constraint ids as lcN. New DEFs therefore carry lc-namespaced local constraints that flow through the unchanged design-story path and are ignored by the c-only synthesizer guard (like k-constraints), so LLD synthesis no longer fails on them. localConstraints[].source stays ^c\\d+$ (a citation reference). The synthesizer guard, the design-story leak point, the DEF renderer, and checkConstraintCoverage are unchanged. Existing on-disk DEFs are not migrated.",
      "migrationSteps": [
        {
          "order": 1,
          "action": "Change the localConstraints[].id property pattern in the define Stories schema from ^c\\d+$ to ^lc\\d+$ (schemas.ts:216). Leave localConstraints[].source at ^c\\d+$.",
          "rollbackable": true
        },
        {
          "order": 2,
          "action": "Align the define runner's Stories prompt guidance (define/index.ts, around :160) so it instructs the model to mint local-constraint ids as lcN and reference them in the lc namespace (not cN), keeping emitted JSON consistent with the tightened schema.",
          "rollbackable": true
        },
        {
          "order": 3,
          "action": "Add/adjust tests: define-schema accepts lc-ids and rejects c-ids for local constraints (source still c); validateCitations ignores lc-refs while still failing dangling c-refs; checkConstraintCoverage + the DEF renderer handle an lc-local-constraint unchanged. Run the workflow suite green.",
          "rollbackable": true
        }
      ],
      "backwardCompat": "No public/runtime API signature changes — this narrows an internal artifact-schema string pattern enforced only at define time. Going-forward only: NEW define runs must emit lc-namespaced local-constraint ids (the aligned prompt handles this; a stray c-id triggers the existing bounded structured-output retry). EXISTING on-disk DEFs that carry c-namespaced local constraints are NOT re-validated or migrated — they remain as-is per the user's explicit choice, and the legacy collision can still fire for those specific DEFs (an accepted, documented boundary, not a new regression). No reader of a persisted DEF breaks: the renderer + checkConstraintCoverage handle any id string generically. The change is fully rollbackable by reverting the pattern + prompt.",
      "zeroDowntime": true,
      "dataRewriteRequired": false
    },
    "alternativesConsidered": [
      {
        "id": "a1",
        "name": "lc-prefix on id only; source stays a c-citation reference",
        "oneLineSummary": "Change localConstraints[].id pattern from ^c\\d+$ to ^lc\\d+$ in the define schema and align the define runner's prompt to mint lcN ids; localConstraints[].source stays ^c\\d+$ (it references a real DEF citation).",
        "chosen": true
      },
      {
        "id": "a2",
        "name": "lc-prefix via a single shared LOCAL_CONSTRAINT_ID pattern constant",
        "oneLineSummary": "Same behavioural change as a1, but extract the local-constraint id pattern into one exported named constant reused by the schema + tests so the lc namespace has a single source of truth.",
        "chosen": false,
        "reasonRejected": "Functionally identical to a1 (fixes the collision, preserves source) but scores partial on minimal-blast-radius: the shared-constant indirection is more machinery than a one-line pattern change warrants in a file that is idiomatically a plain JSON-Schema object literal. A reasonable refinement, not worth the extra surface here."
      },
      {
        "id": "a3",
        "name": "Renamespace BOTH id and source off the c-namespace",
        "oneLineSummary": "Change both localConstraints[].id and localConstraints[].source to ^lc\\d+$, fully decoupling local constraints from the citation namespace.",
        "chosen": false,
        "reasonRejected": "Violates source-grounding-preserved: renamespacing source to lc breaks the semantic that source points at a real DEF citation — the renderer's source emission would land in the ignored lc namespace, so the constraint loses its DEF-citation grounding and becomes unverifiable against the citation list. It also enlarges the blast radius (renderer + source semantics) for no benefit, since the collision was only ever on the echoed id, not on source."
      }
    ],
    "chosenAlternative": "a1",
    "openQuestions": []
  },
  "citations": [
    {
      "id": "c1",
      "kind": "analyze-bundle",
      "ref": "s1 symbol.locate — DEF Stories schema: localConstraints[].id ^c\\d+$ (schemas.ts:216), source ^c\\d+$ (:219), operationalizes ^k\\d+$ (:204); src/workflow/runners/define/schemas.ts"
    },
    {
      "id": "c2",
      "kind": "analyze-bundle",
      "ref": "s1 symbol.locate — the c-only citation guard: CITATION_REF_RE = /\\[\\[c(\\d+)\\]\\]/g (synthesizer.ts:85) + validateCitations hard-fail at :119; src/workflow/synthesizer.ts"
    },
    {
      "id": "c3",
      "kind": "analyze-bundle",
      "ref": "s1 symbol.locate — the design-story leak point: readUpstream returns the whole story incl. localConstraints (index.ts:100), JSON.stringify(story) at 169/212/309/354/454/510; src/workflow/runners/design-story/index.ts"
    },
    {
      "id": "c4",
      "kind": "analyze-bundle",
      "ref": "s1 data-model.trace — DEF renderer (define.ts:170-176, uses c.id generically) + checkConstraintCoverage (define.ts:278-292, localIds branch unreachable for k-only operationalizes); src/workflow/artifacts/define.ts"
    },
    {
      "id": "c5",
      "kind": "analyze-bundle",
      "ref": "s1 search.text — the define runner Stories prompt guidance to align (define/index.ts:160); src/workflow/runners/define/index.ts"
    },
    {
      "id": "c6",
      "kind": "analyze-bundle",
      "ref": "s1 test.locate — define schema/runner tests location (src/workflow/runners/define/__tests__) + artifact renderer tests (src/workflow/artifacts/__tests__)"
    },
    {
      "id": "c7",
      "kind": "analyze-bundle",
      "ref": "s1 test.locate — validateCitations tests location under the workflow suite (src/workflow/__tests__ / synthesizer tests); node:test via npx tsx --test"
    }
  ]
}
