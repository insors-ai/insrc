{
  "meta": {
    "workflow": "plan",
    "runId": "wf-1790193012232-k7xu1h",
    "repoPath": "/Users/subhagho/work/projects/insors/insrc",
    "createdAt": "2026-09-23T19:52:06.223Z",
    "epicCreatedAt": "2026-09-23T17:38:50.877Z",
    "attribution": {
      "outputs": [
        {
          "role": "(client)",
          "tier": "core",
          "runner": "cli-claude",
          "model": "client"
        }
      ]
    },
    "elapsedMs": 113957,
    "repoIndexedAt": null,
    "schemaVersion": 1,
    "epicHash": "b6c90b3e0240d36c",
    "epicSlug": "work-framed-approved-spec-proceed-from",
    "storyId": "s2",
    "lldRunId": "wf-1790190641927-xrt840",
    "lldEffectiveHash": "0a15cb12814dcb165a774cb5a0897725fd968a36cc022f7ae7ecfdfbb00ad01f",
    "approvedAt": "2026-09-23T19:58:18.120Z"
  },
  "body": {
    "tasks": [
      {
        "id": "t1",
        "title": "Build the pure installed-commit reader (readInstalledCommit) over an injected exec seam",
        "summary": "New module src/daemon/installed-commit.ts exporting readInstalledCommit(root?, deps?): resolve DAEMON_ROOT ($INSRC_DAEMON_ROOT ?? ~/.insrc/daemon, its own env-var+default — NOT importing s1's update-runner) and run `git -C <root> rev-parse HEAD` via an injected exec seam (default execFileSync-based), .trim() the sha, and return '' on ANY failure (non-git/missing root, git ENOENT, non-zero exit, thrown exec) inside try/catch so it NEVER throws. Mirrors the sync execFileSync git idiom (migrate-docs-tree.ts:317) + the exact rev-parse HEAD command (maintenance.ts:101).",
        "size": "S",
        "order": 1,
        "dependsOn": [],
        "acceptanceChecks": [
          "readInstalledCommit(root, {exec}) returns the trimmed sha for a valid HEAD (trailing newline stripped)",
          "the injected exec is invoked with git args resolving to `-C <root> rev-parse HEAD`",
          "any exec failure (throw / non-git / ENOENT) returns '' and never throws",
          "with no root arg it resolves DAEMON_ROOT = $INSRC_DAEMON_ROOT ?? ~/.insrc/daemon",
          "does NOT import src/daemon/update-runner.ts (sc2 independent of sc1); tsc clean"
        ],
        "derivedFrom": [
          "c1",
          "c2"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "readInstalledCommit: injected exec returning a sha (+ trailing newline) -> the bare trimmed sha"
          },
          {
            "level": "unit",
            "name": "readInstalledCommit: the injected exec is invoked with git args resolving to `-C <root> rev-parse HEAD`"
          },
          {
            "level": "unit",
            "name": "readInstalledCommit: an exec that THROWS (non-git / non-zero exit / ENOENT) -> '' , never throws"
          },
          {
            "level": "unit",
            "name": "readInstalledCommit: no root arg resolves DAEMON_ROOT = $INSRC_DAEMON_ROOT ?? ~/.insrc/daemon (via an INSRC_DAEMON_ROOT env override)"
          }
        ]
      },
      {
        "id": "t2",
        "title": "Add the additive DaemonStatus.installedCommit field + wire the daemon.status handler",
        "summary": "Add installedCommit:string to the DaemonStatus interface (src/shared/types.ts, at the existing DaemonStatus — unshifted by S001) and set it in the daemon.status handler by calling readInstalledCommit(). Locate the handler BY NAME (the 'daemon.status': async () => {...} entry carrying the 'best-effort; status should never throw' fs-stat catch, now ~src/daemon/index.ts:944 after S001's line drift), NOT the stale :922 anchor. The handler needs no new try/catch — readInstalledCommit itself never throws — so daemon.status's never-throw guarantee is preserved. Additive only: no other field changes, sc1 (daemon.update/updateOutcome) untouched.",
        "size": "S",
        "order": 2,
        "dependsOn": [
          "t1"
        ],
        "acceptanceChecks": [
          "src/shared/types.ts DaemonStatus carries a required installedCommit:string field",
          "the daemon.status handler sets installedCommit from readInstalledCommit() and returns it on the status object",
          "a reader returning '' yields installedCommit:'' with the rest of the status intact (handler never throws)",
          "no other DaemonStatus field changed; daemon.update/updateOutcome (sc1) untouched",
          "tsc clean across daemon + shared"
        ],
        "derivedFrom": [
          "c3",
          "c4"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "daemon.status status object includes installedCommit populated from the (injected/stub) reader"
          },
          {
            "level": "unit",
            "name": "daemon.status with a reader returning '' yields installedCommit:'' with the rest of the status intact and never throws"
          },
          {
            "level": "unit",
            "name": "daemon.status change leaves sc1 fields (daemon.update/updateOutcome) untouched"
          }
        ]
      }
    ],
    "testStrategyCoverage": [
      {
        "lldStrategyItem": "readInstalledCommit with an exec seam returning a sha (+ trailing newline) -> the bare trimmed sha",
        "coveredByTaskIds": [
          "t1"
        ]
      },
      {
        "lldStrategyItem": "readInstalledCommit runs `git -C <root> rev-parse HEAD` — the injected exec records the exact command+argv",
        "coveredByTaskIds": [
          "t1"
        ]
      },
      {
        "lldStrategyItem": "readInstalledCommit with an exec seam that THROWS (non-git root / non-zero exit / git ENOENT) -> '' , never throws",
        "coveredByTaskIds": [
          "t1"
        ]
      },
      {
        "lldStrategyItem": "readInstalledCommit with no root arg resolves DAEMON_ROOT = $INSRC_DAEMON_ROOT ?? ~/.insrc/daemon",
        "coveredByTaskIds": [
          "t1"
        ]
      },
      {
        "lldStrategyItem": "the status object includes installedCommit populated from the (injected) reader",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "a reader that returns '' (undeterminable) yields installedCommit:'' with the rest of the status intact — the handler still resolves, never throws",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "sc1 fields (daemon.update/updateOutcome) are untouched by the status change",
        "coveredByTaskIds": [
          "t2"
        ]
      }
    ]
  },
  "citations": [
    {
      "id": "c1",
      "kind": "prior-artifact",
      "ref": "LLD s2 dataModel: readInstalledCommit new pure module (src/daemon/installed-commit.ts) over an injected exec seam, best-effort, never throws"
    },
    {
      "id": "c2",
      "kind": "convention",
      "ref": "s1 convention: sync execFileSync git idiom (src/workflow/migrate-docs-tree.ts:317) + exact `git -C <root> rev-parse HEAD` (src/cli/services/maintenance.ts:101) + DAEMON_ROOT (maintenance.ts:34)"
    },
    {
      "id": "c3",
      "kind": "prior-artifact",
      "ref": "LLD s2 dataModel: DaemonStatus field-add installedCommit:string (src/shared/types.ts) + sc2 interaction"
    },
    {
      "id": "c4",
      "kind": "analyze-bundle",
      "ref": "s1 sizing: daemon.status handler located by name (the 'status should never throw' block, ~src/daemon/index.ts:944 post-S001-drift) sets installedCommit; never-throw preserved"
    }
  ]
}
