{
  "meta": {
    "workflow": "plan",
    "runId": "wf-1786001840345-jh3nkw",
    "repoPath": "/Users/subhagho/work/projects/insors/insrc",
    "createdAt": "2026-08-06T07:43:46.670Z",
    "attribution": {
      "outputs": [
        {
          "role": "(client)",
          "tier": "core",
          "runner": "cli-claude",
          "model": "client"
        }
      ]
    },
    "elapsedMs": 386298,
    "repoIndexedAt": null,
    "schemaVersion": 1,
    "epicHash": "f900cf34c0342d4f",
    "epicSlug": "seed-from-approved-specartifact-spec-bfa1f6c830c53982",
    "storyId": "s3",
    "lldRunId": "wf-1786000128848-p3vzsl",
    "lldEffectiveHash": "ddca67745f5ce27a72fd98622c88c10523b7fb157faee13556f302030c37eece",
    "review": {
      "artifact": "plan",
      "stage": "plan",
      "verdict": "pass",
      "findings": [
        {
          "claimId": "cl1",
          "ref": "t2",
          "kind": "citation",
          "severity": "LOW",
          "premise": "DAEMON_ENTRY is a module-private const in src/cli/services/daemon.ts (around line 24) that t2 exports.",
          "evidence": "src/cli/services/daemon.ts:24 reads `const DAEMON_ENTRY = join(__dirname, '../../daemon/index.js');` — module-private const exactly as cited; t2's export is well-founded.",
          "action": "None — citation confirmed verbatim.",
          "fixability": "auto"
        },
        {
          "claimId": "cl2",
          "ref": "t2",
          "kind": "citation",
          "severity": "LOW",
          "premise": "pidFromFile is a module-private function in src/cli/services/lifecycle.ts (around line 20) that t2 exports.",
          "evidence": "src/cli/services/lifecycle.ts:20 reads `function pidFromFile(): number | undefined {` — module-private fn exactly as cited; t2's export is well-founded.",
          "action": "None — citation confirmed verbatim.",
          "fixability": "auto"
        },
        {
          "claimId": "cl3",
          "ref": "t1",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/services/debug-types.ts exists and hosts the sc1 DebugService types where t1 colocates the new orphan types.",
          "evidence": "grep finds DebugService/DaemonCardModel in src/cli/services/debug-types.ts (the sc1 types host); colocation target exists.",
          "action": "None — target file confirmed.",
          "fixability": "auto"
        },
        {
          "claimId": "cl4",
          "ref": "t3",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/services/debug.ts exists and hosts the debug service impl (buildDaemonCard/daemonStatus) that t3 extends with scanOrphansWith/killOrphansWith.",
          "evidence": "grep finds buildDaemonCard/realDeps in src/cli/services/debug.ts; the injectable-deps service module t3 extends exists.",
          "action": "None — target file confirmed.",
          "fixability": "auto"
        },
        {
          "claimId": "cl5",
          "ref": "t4",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/panes/DaemonSection.tsx exists (the s2 status-card component) and is read-only today, which t4 grows with the orphan area.",
          "evidence": "grep finds DaemonSection in src/cli/panes/DaemonSection.tsx (the s2 component); t4's growth target exists.",
          "action": "None — target file confirmed.",
          "fixability": "auto"
        },
        {
          "claimId": "cl6",
          "ref": "t4",
          "kind": "citation",
          "severity": "LOW",
          "premise": "ConfirmPrompt is an exported widget with its own useInput (y/n/esc) that t4 uses to gate the kill.",
          "evidence": "src/cli/ui/widgets.tsx:56 reads `export function ConfirmPrompt(props: { label: string; onYes: () => void; onNo: () => void }): ReactElement {` — the confirm widget t4 gates the kill with exists exactly as cited.",
          "action": "None — citation confirmed verbatim.",
          "fixability": "auto"
        },
        {
          "claimId": "cl7",
          "ref": "t5",
          "kind": "inventory",
          "severity": "LOW",
          "premise": "The in-repo debug facade fakes needing scanOrphans/killOrphans stubs live in exactly these four test files: tui.test.ts, command.test.ts, DebugPane.test.ts, DaemonSection.test.ts.",
          "evidence": "grep for daemonStatus/debug: matches across the four named test files (tui/command/DebugPane/DaemonSection); the inventory of in-repo debug fakes to update is accurate.",
          "action": "None — fake inventory confirmed.",
          "fixability": "auto"
        },
        {
          "claimId": "cl8",
          "ref": "t3",
          "kind": "semantic",
          "severity": "LOW",
          "premise": "A ps-based process listing precedent using execSync/execFileSync exists in the CLI/shared layer, supporting the injectable ps-runner over `ps -o pid,command`.",
          "evidence": "grep confirms child_process/execSync precedent in the shared layer (src/shared/system-info.ts); the injectable ps-runner over `ps -o pid,command` has an existing pattern to follow.",
          "action": "None — ps precedent confirmed.",
          "fixability": "auto"
        },
        {
          "claimId": "cl9",
          "ref": "t3",
          "kind": "ordering",
          "severity": "LOW",
          "premise": "t3 depends on t1 (the orphan types) and t2 (the exported DAEMON_ENTRY + pidFromFile); t4 depends on t1+t3; t5 depends on t3 — an acyclic order matching the build sequence.",
          "evidence": "dependsOn graph t1,t2 (roots) -> t3 -> {t4,t5} is acyclic with order 1..5 a valid topological sort; matches the checklist.verify audit.",
          "action": "None — ordering is consistent and acyclic.",
          "fixability": "auto"
        }
      ],
      "counts": {
        "high": 0,
        "med": 0,
        "low": 9
      },
      "reviewedAt": "2026-08-06T07:45:36.648Z",
      "model": "client"
    },
    "approvedAt": "2026-08-06T07:48:41.657Z"
  },
  "body": {
    "tasks": [
      {
        "id": "t1",
        "title": "Add the OrphanScanResult / OrphanProcess / KillOutcome types",
        "summary": "Add the three view-model types to src/cli/services/debug-types.ts (colocated with the sc1 types): OrphanScanResult = { supported:true; orphans: readonly OrphanProcess[] } | { supported:false }; OrphanProcess { readonly pid:number; readonly command:string }; KillOutcome { readonly pid:number; readonly result:'terminated'|'forced'|'not-found'|'error' }. Additive type surface, referenced by nothing yet.",
        "size": "S",
        "order": 1,
        "dependsOn": [],
        "acceptanceChecks": [
          "OrphanScanResult is discriminated on `supported`; the unsupported variant is exactly { supported:false }",
          "OrphanProcess is {pid,command}; KillOutcome is {pid,result} with the four-member result union",
          "tsc compiles the new types"
        ],
        "derivedFrom": [
          "c1"
        ],
        "tests": [
          {
            "level": "integration",
            "name": "debug-types.test.ts: OrphanScanResult/OrphanProcess/KillOutcome are exercised by a type-level round-trip (supported + unsupported variants, four KillOutcome results) and the suite compiles"
          }
        ]
      },
      {
        "id": "t2",
        "title": "Export DAEMON_ENTRY + pidFromFile for reuse",
        "summary": "Add `export` to the module-private DAEMON_ENTRY const (src/cli/services/daemon.ts:24) and pidFromFile function (src/cli/services/lifecycle.ts:20) so the debug service reuses the same resolved daemon-entry path + managed-pid read. Visibility-only; no behaviour change to existing callers.",
        "size": "S",
        "order": 2,
        "dependsOn": [],
        "acceptanceChecks": [
          "DAEMON_ENTRY is exported from daemon.ts (value unchanged)",
          "pidFromFile is exported from lifecycle.ts (behaviour unchanged)",
          "tsc compiles; existing daemon.ts/lifecycle.ts callers are unaffected"
        ],
        "derivedFrom": [
          "c2"
        ],
        "tests": [
          {
            "level": "integration",
            "name": "debug.ts imports DAEMON_ENTRY + pidFromFile — verified transitively by the scanOrphansWith/killOrphansWith unit suite compiling and the existing daemon/lifecycle suites staying green"
          }
        ]
      },
      {
        "id": "t3",
        "title": "Implement scanOrphansWith/killOrphansWith + the scanOrphans/killOrphans facade methods",
        "summary": "In src/cli/services/debug.ts add injectable-deps helpers: scanOrphansWith(deps) (deps: ps-runner, platform, managedPid, daemonEntry) runs the ps scan, matches the resolved DAEMON_ENTRY, excludes the managed pid, returns {supported:false} on win32, and folds any ps failure into {supported:true; orphans:[]}; killOrphansWith(deps) (deps: kill(pid,signal), isAlive/liveness probe, wait/clock, platform, managedPid) does SIGTERM -> wait ~3s -> SIGKILL-only-for-survivors over exactly the passed pids, re-excludes the managed pid, folds signal errors into per-pid KillOutcome, no-ops off-POSIX, never throws. Add zero-arg wrappers scanOrphans()/killOrphans(pids) over realDeps (DAEMON_ENTRY, pidFromFile, execFileSync ps, process.kill, process.platform), add the two methods to the DebugService interface, and bind them onto the makeServices() debug entry. Build note: land WITH t5 (the fakes) in one increment.",
        "size": "L",
        "order": 3,
        "dependsOn": [
          "t1",
          "t2"
        ],
        "acceptanceChecks": [
          "DebugService interface gains scanOrphans(): OrphanScanResult + killOrphans(pids: readonly number[]): Promise<KillOutcome[]>; existing members unchanged",
          "scanOrphansWith returns non-managed daemon-entry pids from canned ps output; {supported:false} on win32; {supported:true;orphans:[]} on ps failure",
          "killOrphansWith sends SIGTERM then SIGKILL only to survivors (injected clock), acts only on the passed pids, re-excludes the managed pid, folds errors into per-pid KillOutcome, never throws",
          "makeServices().debug.scanOrphans + .killOrphans are bound to the real impls; tsc compiles",
          "no real ps/process.kill runs in the unit tests (deps injected)"
        ],
        "derivedFrom": [
          "c1",
          "c2",
          "c3"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "scanOrphansWith: canned ps output (daemon-entry + managed-pid + unrelated node) -> {supported:true} with ONLY the non-managed daemon-entry pid(s)"
          },
          {
            "level": "unit",
            "name": "scanOrphansWith: platform 'win32' -> {supported:false} with no ps invoked"
          },
          {
            "level": "unit",
            "name": "scanOrphansWith: throwing/garbled ps-runner -> {supported:true; orphans:[]} (never throws)"
          },
          {
            "level": "unit",
            "name": "scanOrphansWith: managed pid undefined -> every matching daemon-entry process is a candidate"
          },
          {
            "level": "unit",
            "name": "killOrphansWith([p]) p dies on SIGTERM -> (p,'SIGTERM') then liveness clears; 'terminated', no SIGKILL"
          },
          {
            "level": "unit",
            "name": "killOrphansWith([p]) p survives -> (p,'SIGTERM') then after injected wait (p,'SIGKILL'); 'forced'"
          },
          {
            "level": "unit",
            "name": "killOrphansWith([managedPid,p]) -> managed pid never signalled; only p processed"
          },
          {
            "level": "unit",
            "name": "killOrphansWith([p]) kill throws EPERM/ESRCH -> 'error'/'not-found', no re-throw, other pids still processed"
          },
          {
            "level": "unit",
            "name": "killOrphansWith([]) -> no kill calls, resolves []; off-POSIX -> no-op"
          },
          {
            "level": "integration",
            "name": "makeServices().debug exposes scanOrphans + killOrphans bound to the real impls"
          }
        ]
      },
      {
        "id": "t4",
        "title": "Grow the DaemonSection with the orphan area (multi-select + ConfirmPrompt-gated kill)",
        "summary": "In src/cli/panes/DaemonSection.tsx add an orphan area below the s2 status card: read scanOrphans() (on mount/refresh), render the recommendation rows (pid + command) with a pane-local multi-select (Set<number>) + a refresh key via a new useInput (gated on !useCaptured() AND not while confirming, so the ConfirmPrompt's own useInput owns keys during confirm); a kill-request opens ConfirmPrompt (only when the selection is non-empty), and only on explicit Yes calls killOrphans(selectedPids) and renders the per-pid outcomes. Non-POSIX (scanOrphans -> {supported:false}) renders a 'not supported on this platform' line with no scan/kill control. Empty scan -> 'no orphan processes found'. Read-only except this one guarded kill.",
        "size": "L",
        "order": 4,
        "dependsOn": [
          "t1",
          "t3"
        ],
        "acceptanceChecks": [
          "POSIX: renders scanOrphans() rows as recommendations; empty -> 'no orphan processes found'",
          "selecting pids + kill-request opens ConfirmPrompt; No never calls killOrphans; Yes calls killOrphans with EXACTLY the selected pids and shows the per-pid outcomes",
          "a kill-request with an empty selection is inert (killOrphans not invoked)",
          "non-POSIX ({supported:false}) shows 'not supported on this platform' with no scan/kill control",
          "the section's useInput is suspended while the ConfirmPrompt is open (no double key handling); the s2 status card is unchanged; tsc compiles"
        ],
        "derivedFrom": [
          "c1",
          "c4"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "DaemonSection orphan area (POSIX): renders scanOrphans() rows (pid + command); empty -> 'no orphan processes found'"
          },
          {
            "level": "unit",
            "name": "DaemonSection: select pids + kill-request opens ConfirmPrompt; No never calls killOrphans; Yes calls killOrphans with EXACTLY the selected pids + shows per-pid outcomes"
          },
          {
            "level": "unit",
            "name": "DaemonSection: kill-request with an empty selection is inert (killOrphans not invoked)"
          },
          {
            "level": "unit",
            "name": "DaemonSection (non-POSIX, {supported:false}): renders 'not supported on this platform' with no scan/kill control"
          }
        ]
      },
      {
        "id": "t5",
        "title": "Update the in-repo debug-facade test fakes for scanOrphans/killOrphans",
        "summary": "Add conformant scanOrphans + killOrphans stubs to every in-repo `debug` facade fake so the widened DebugService compiles: fakeServices() in src/cli/__tests__/tui.test.ts, ctxWith base in src/cli/__tests__/command.test.ts, and the props built in src/cli/panes/__tests__/DebugPane.test.ts + DaemonSection.test.ts (default scanOrphans -> {supported:false}, killOrphans -> []). Build note: apply together-with t3 in ONE increment (mirrors s1/t5 + s2/t5 lockstep); dependsOn stays [t3] (the true compile dependency).",
        "size": "S",
        "order": 5,
        "dependsOn": [
          "t3"
        ],
        "acceptanceChecks": [
          "every in-repo debug-facade fake includes scanOrphans + killOrphans stubs returning valid OrphanScanResult / KillOutcome[]",
          "`npx tsx --test 'src/cli/**/*.test.ts'` compiles against the widened DebugService",
          "no existing assertion behaviour changes beyond the additive stubs"
        ],
        "derivedFrom": [
          "c5"
        ],
        "tests": [
          {
            "level": "integration",
            "name": "the in-repo debug fakes (tui/command/DebugPane/DaemonSection tests) carry scanOrphans + killOrphans stubs so the CLI suite compiles"
          }
        ]
      }
    ],
    "testStrategyCoverage": [
      {
        "lldStrategyItem": "scanOrphansWith: canned ps output with a daemon-entry line + the managed-pid line + an unrelated node line -> { supported:true; orphans } containing ONLY the non-managed daemon-entry pid(s)",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "scanOrphansWith: platform 'win32' -> { supported:false } (no ps invoked)",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "scanOrphansWith: a throwing/garbled ps-runner -> { supported:true; orphans:[] } (never throws)",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "scanOrphansWith: managed pid undefined (daemon down) -> every matching daemon-entry process is a candidate (nothing excluded on that basis)",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "killOrphansWith([p]) where p dies on SIGTERM -> kill called with (p,'SIGTERM') then liveness-probe clears; outcome 'terminated', NO SIGKILL sent",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "killOrphansWith([p]) where p survives -> (p,'SIGTERM') then after the injected wait (p,0) shows alive -> (p,'SIGKILL'); outcome 'forced'",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "killOrphansWith([managedPid, p]) -> the managed pid is re-excluded/never signalled; only p is processed (k5)",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "killOrphansWith([p]) where kill throws EPERM/ESRCH -> outcome 'error'/'not-found', no re-throw, other pids still processed",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "killOrphansWith([]) -> no kill calls, resolves []; and off-POSIX -> no-op",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "DaemonSection orphan area (POSIX): renders scanOrphans() rows (pid + command) as recommendations; empty -> 'no orphan processes found'",
        "coveredByTaskIds": [
          "t4"
        ]
      },
      {
        "lldStrategyItem": "DaemonSection: selecting pids + requesting kill opens a ConfirmPrompt; choosing No never calls killOrphans; choosing Yes calls killOrphans with EXACTLY the selected pids and shows the per-pid outcomes",
        "coveredByTaskIds": [
          "t4"
        ]
      },
      {
        "lldStrategyItem": "DaemonSection: a kill request with an empty selection is inert (killOrphans not invoked)",
        "coveredByTaskIds": [
          "t4"
        ]
      },
      {
        "lldStrategyItem": "DaemonSection (non-POSIX, scanOrphans -> {supported:false}): renders 'not supported on this platform' with no scan/kill control",
        "coveredByTaskIds": [
          "t4"
        ]
      },
      {
        "lldStrategyItem": "makeServices().debug exposes scanOrphans + killOrphans bound to the real impls",
        "coveredByTaskIds": [
          "t3"
        ]
      },
      {
        "lldStrategyItem": "the in-repo debug fakes (tui/command/DebugPane/DaemonSection tests) carry scanOrphans + killOrphans stubs so the suite compiles",
        "coveredByTaskIds": [
          "t5"
        ]
      }
    ]
  },
  "citations": [
    {
      "id": "c1",
      "kind": "prior-artifact",
      "ref": "LLD s3 handoff: OrphanScanResult/OrphanProcess/KillOutcome view-model types + the scan recommendation model (lc1: scan is recommendation-only)"
    },
    {
      "id": "c2",
      "kind": "prior-artifact",
      "ref": "LLD s3 handoff: reuse the resolved DAEMON_ENTRY (daemon.ts:24) + managed-pid read pidFromFile (lifecycle.ts:20) via export to identify + exclude the daemon's own process (k1/k5)"
    },
    {
      "id": "c3",
      "kind": "prior-artifact",
      "ref": "LLD s3 handoff: killOrphansWith SIGTERM->wait->SIGKILL-only-for-survivors escalation over exactly the selected pids, injectable ps/kill/clock/platform/managed-pid deps, POSIX-only degrade, never throws (k2/k3/lc1)"
    },
    {
      "id": "c4",
      "kind": "prior-artifact",
      "ref": "LLD s3 handoff: DaemonSection orphan area — multi-select + ConfirmPrompt-gated kill acting solely on explicit selection + confirm, useInput suspended while confirming, non-POSIX 'not supported' degrade (k2/k3/lc1; ConfirmPrompt widgets.tsx:56)"
    },
    {
      "id": "c5",
      "kind": "prior-artifact",
      "ref": "LLD s3 handoff: the widened DebugService facade requires the in-repo debug fakes (tui/command/DebugPane/DaemonSection tests) to carry conformant scanOrphans/killOrphans stubs (t3+t5 lockstep, mirrors sc1 flat-facade extension)"
    }
  ]
}
