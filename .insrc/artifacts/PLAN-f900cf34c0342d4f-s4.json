{
  "meta": {
    "workflow": "plan",
    "runId": "wf-1786004445363-hpyask",
    "repoPath": "/Users/subhagho/work/projects/insors/insrc",
    "createdAt": "2026-08-06T08:25:37.782Z",
    "attribution": {
      "outputs": [
        {
          "role": "(client)",
          "tier": "core",
          "runner": "cli-claude",
          "model": "client"
        }
      ]
    },
    "elapsedMs": 292395,
    "repoIndexedAt": null,
    "schemaVersion": 1,
    "epicHash": "f900cf34c0342d4f",
    "epicSlug": "seed-from-approved-specartifact-spec-bfa1f6c830c53982",
    "storyId": "s4",
    "lldRunId": "wf-1786003781032-qjmy7p",
    "lldEffectiveHash": "ddca67745f5ce27a72fd98622c88c10523b7fb157faee13556f302030c37eece",
    "review": {
      "artifact": "plan",
      "stage": "plan",
      "verdict": "pass",
      "findings": [
        {
          "claimId": "cl1",
          "ref": "t1",
          "kind": "citation",
          "severity": "LOW",
          "premise": "IpcRequest is defined in src/shared/types.ts around line 749 (the additive client field target).",
          "evidence": "src/shared/types.ts:749 reads `export interface IpcRequest {` — the additive client-envelope target confirmed.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl2",
          "ref": "t2",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/daemon/server.ts IpcServer has handleConnection + handleMessage and a constructor taking handlers + streamHandlers (the registry hook points + the 3rd now-clock arg target).",
          "evidence": "src/daemon/server.ts:75 `private handleConnection(socket: Socket): void {` confirmed; class IpcServer + handleMessage exist as the registry hook points.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl3",
          "ref": "t3",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/daemon/index.ts constructs `new IpcServer(...)` (~line 452) and registers the 'daemon.status' handler (~line 849), the peer site for daemon.debug-status.",
          "evidence": "src/daemon/index.ts:452 `const server = new IpcServer({` and :849 `'daemon.status': async () => {` — both the IpcServer construction site (holds the ref for attachedClients()) and the peer handler location confirmed.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl4",
          "ref": "t4",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/client.ts rpc() writes an IpcRequest object (~line 17) — the single site to attach the client envelope.",
          "evidence": "src/cli/client.ts:11 confirms rpc(); the request-write site (const req: IpcRequest) is where the client envelope attaches.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl5",
          "ref": "t5",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/services/debug-types.ts hosts the DebugService interface (with s2 daemonStatus + s3 scanOrphans/killOrphans) that t5 widens, and src/cli/services/debug.ts holds the injectable-deps cores + realDeps.",
          "evidence": "src/cli/services/debug-types.ts:35 `export interface DebugService {` confirmed — the facade t5 widens (with s2/s3 members present); debug.ts holds the injectable-deps cores.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl6",
          "ref": "t5",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/daemon/mcp-register.ts provides runCli + alreadyRegistered + a two-client (claude, codex) ClientPlan list that the client-side mcpStatusWith parse reuses.",
          "evidence": "src/daemon/mcp-register.ts:105 `const plans: ClientPlan[] = [` with the claude + codex entries confirmed; runCli/alreadyRegistered present — the two-client parse shape mcpStatusWith reuses.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl7",
          "ref": "t6",
          "kind": "citation",
          "severity": "LOW",
          "premise": "src/cli/panes/DebugPane.tsx has a SECTION_VIEWS mcp placeholder ('MCP diagnostics') that t6 swaps for <MCPSection/>, mirroring how the daemon section is wired.",
          "evidence": "src/cli/panes/DebugPane.tsx:36 `const SECTION_VIEWS: Partial<Record<string, () => ReactElement>> = {` confirmed — the mcp placeholder swap target (mirrors the daemon-section wiring).",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl8",
          "ref": "t7",
          "kind": "inventory",
          "severity": "LOW",
          "premise": "The in-repo debug facade fakes needing attachedClients/mcpStatus stubs live in exactly these test files: tui.test.ts, command.test.ts, DebugPane.test.ts, DaemonSection.test.ts (plus the new MCPSection test).",
          "evidence": "grep for daemonStatus:/scanOrphans: matches across the four named test files (tui/command/DebugPane/DaemonSection); the fake inventory to widen is accurate (the new MCPSection test adds its own fake).",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl9",
          "ref": "t2",
          "kind": "closed-union",
          "severity": "LOW",
          "premise": "The IpcServer socket lifecycle exposes a 'close' event (used to delete the registry entry) alongside the existing 'data'/'error' handlers in handleConnection.",
          "evidence": "server.ts handleConnection registers socket.on('data')+('error'); the stream path already uses socket.on('close') at server.ts:149, so the 'close' lifecycle event t2 relies on for registry deletion is available.",
          "action": "None.",
          "fixability": "auto"
        },
        {
          "claimId": "cl10",
          "ref": "plan",
          "kind": "ordering",
          "severity": "LOW",
          "premise": "The task dependency graph is acyclic (t1 root; t2<-t1; t3<-t2; t4<-t1; t5<-t1; t6<-t5; t7<-t5) with order 1..7 a valid topological sort.",
          "evidence": "dependency graph t1 root -> {t2,t4,t5}; t2->t3; t5->{t6,t7} is acyclic with order 1..7 a valid topological sort; matches the checklist.verify audit.",
          "action": "None — ordering consistent.",
          "fixability": "auto"
        }
      ],
      "counts": {
        "high": 0,
        "med": 0,
        "low": 10
      },
      "reviewedAt": "2026-08-06T08:26:42.135Z",
      "model": "client"
    },
    "approvedAt": "2026-08-06T08:28:47.107Z"
  },
  "body": {
    "tasks": [
      {
        "id": "t1",
        "title": "Add IpcRequest.client envelope + AttachedClient shared type",
        "summary": "In src/shared/types.ts add the OPTIONAL `client?: { readonly label: string; readonly pid: number }` field to IpcRequest and the new `AttachedClient { readonly id: number; readonly label: string; readonly pid?: number; readonly connectedAt: number; readonly lastMethod?: string }` interface. Purely additive — every existing caller/handler stays valid with the field absent.",
        "size": "S",
        "order": 1,
        "dependsOn": [],
        "acceptanceChecks": [
          "IpcRequest gains an optional `client` object (readonly label + pid); existing fields unchanged and every current rpc call still type-checks",
          "AttachedClient is exported from shared/types.ts with id/label/pid?/connectedAt/lastMethod?",
          "tsc compiles"
        ],
        "derivedFrom": [
          "c1",
          "c2"
        ],
        "tests": [
          {
            "level": "integration",
            "name": "IpcRequest.client is optional/additive: an existing rpc call with no client envelope still type-checks (whole-tree tsc)"
          }
        ]
      },
      {
        "id": "t2",
        "title": "Add the in-memory connection registry to IpcServer",
        "summary": "In src/daemon/server.ts add a private Map<Socket,ConnInfo> + a monotonic id counter + an injected `now` clock (3rd constructor arg, defaulting to a real epoch-ms clock). handleConnection creates an entry (id, connectedAt=now(), label 'unknown') and adds a socket.on('close') that deletes it; handleMessage updates the entry from a VALIDATED request.client (finite positive pid + non-empty bounded label, else ignored) + request.method (lastMethod). Add public attachedClients(): AttachedClient[] returning the snapshot sorted by connectedAt. Read-only, in-memory; no existing request-handling behaviour changes, and no code path ever closes a peer connection (k5).",
        "size": "M",
        "order": 2,
        "dependsOn": [
          "t1"
        ],
        "acceptanceChecks": [
          "A new connection registers an entry (monotonic id + connectedAt from the injected clock), labelled 'unknown' until a message identifies it",
          "handleMessage with request.client={label,pid}+method updates label/pid/lastMethod; a malformed client envelope is ignored (stays 'unknown'/pid undefined)",
          "socket 'close' deletes the entry; a double-close / close-before-message is an idempotent no-op (no throw)",
          "attachedClients() returns entries sorted by connectedAt and never mutates or closes a connection (k5)",
          "Existing data/error handling + stream handling is unchanged; tsc compiles"
        ],
        "derivedFrom": [
          "c3"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "IpcServer registry: a new connection creates an entry (monotonic id + injected-clock connectedAt), labelled 'unknown' until a message identifies it"
          },
          {
            "level": "unit",
            "name": "IpcServer registry: handleMessage with request.client={label,pid}+method updates label/pid/lastMethod; attachedClients() reflects it"
          },
          {
            "level": "unit",
            "name": "IpcServer registry: socket 'close' deletes the entry; double-close / close-before-message is an idempotent no-op"
          },
          {
            "level": "unit",
            "name": "IpcServer registry: a malformed request.client (missing/non-integer pid, empty/oversized label) is ignored — stays 'unknown'/pid undefined"
          },
          {
            "level": "unit",
            "name": "IpcServer.attachedClients() returns entries sorted by connectedAt and never mutates/closes any connection (k5)"
          }
        ]
      },
      {
        "id": "t3",
        "title": "Register the read-only daemon.debug-status handler",
        "summary": "In src/daemon/index.ts register a new 'daemon.debug-status' RpcHandler as a peer of 'daemon.status' in the handler map, returning { clients: server.attachedClients() } from the IpcServer instance constructed at index.ts:452. Read-only; no side effects, no persistence.",
        "size": "S",
        "order": 3,
        "dependsOn": [
          "t2"
        ],
        "acceptanceChecks": [
          "'daemon.debug-status' is registered in the index.ts handler map and returns { clients: AttachedClient[] } from the live IpcServer registry",
          "The handler has no side effects and never closes/mutates a connection (k1/k5)",
          "tsc compiles; existing handlers untouched"
        ],
        "derivedFrom": [
          "c4"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "daemon.debug-status handler returns { clients } from a stub IpcServer.attachedClients() and has no side effects (k1/k5)"
          }
        ]
      },
      {
        "id": "t4",
        "title": "Populate the client identity envelope in the rpc + mcp producers",
        "summary": "Set IpcRequest.client on the producer path(s): src/cli/client.ts rpc() writes `client: { label: 'cli', pid: process.pid }` (client.ts:17); if the insrc-mcp server has its OWN daemon-rpc client, it writes `client: { label: 'mcp:<clientName>', pid: process.pid }`. Build note: first grep for the insrc-mcp daemon-client call site — if it reuses cli/client.ts rpc() there is no second edit and t4 collapses to the rpc() change (CLI connections still labelled). Producers only — no reader/behaviour change; an unlabelled producer simply shows 'unknown'.",
        "size": "S",
        "order": 4,
        "dependsOn": [
          "t1"
        ],
        "acceptanceChecks": [
          "rpc() attaches client={label:'cli', pid:process.pid} to every request it writes",
          "If the insrc-mcp server has a distinct daemon-client, it attaches client={label:'mcp:<client>', pid:process.pid}; if it reuses rpc(), that is documented and sufficient",
          "No external signature/behaviour change to rpc(); tsc compiles"
        ],
        "derivedFrom": [
          "c1",
          "c5"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "rpc() writes an IpcRequest carrying client={label:'cli', pid:process.pid} (asserted against a captured socket write with an injected connection seam)"
          }
        ]
      },
      {
        "id": "t5",
        "title": "Widen the DebugService facade with attachedClients + mcpStatus (types + impls + binding)",
        "summary": "Add DebugStatusModel (discriminated reachable/unreachable) + McpClientStatus to src/cli/services/debug-types.ts and widen the DebugService interface with attachedClients(): Promise<DebugStatusModel> + mcpStatus(): Promise<readonly McpClientStatus[]>. In src/cli/services/debug.ts add injectable-deps cores: attachedClientsWith(deps) (rpc seam → folds resolve to {reachable:true;clients sorted}, reject to {reachable:false}, never throws) and mcpStatusWith(deps) (per claude/codex, injectable runCli → parse insrc-line=registered + connection-state token=connected, spawnError/non-zero → available:false, never throws, always one entry per client) + zero-arg facade wrappers over realDeps, bound onto the makeServices() debug entry. Build note: land WITH t7 (the fakes) in one increment.",
        "size": "L",
        "order": 5,
        "dependsOn": [
          "t1"
        ],
        "acceptanceChecks": [
          "DebugService interface gains attachedClients() + mcpStatus(); DebugStatusModel + McpClientStatus are exported from debug-types.ts",
          "attachedClientsWith folds a resolving rpc → {reachable:true;clients} (sorted) and a rejecting rpc → {reachable:false} without throwing",
          "mcpStatusWith returns exactly one McpClientStatus per claude/codex from canned mcp-list output (registered + connected independent); a spawnError/non-zero client → available:false; never throws",
          "makeServices().debug.attachedClients + .mcpStatus are bound to the real impls; no real subprocess/socket runs in unit tests (deps injected)",
          "tsc compiles"
        ],
        "derivedFrom": [
          "c2",
          "c6",
          "c7",
          "c8"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "mcpStatusWith: canned claude+codex mcp-list output with an insrc line + connection-state token -> per client {registered:true, connected:<parsed>, available:true}"
          },
          {
            "level": "unit",
            "name": "mcpStatusWith: stdout with NO insrc line -> {registered:false, connected:false, available:true}"
          },
          {
            "level": "unit",
            "name": "mcpStatusWith: a runner spawnError / non-zero exit for one client -> {available:false,...} while the OTHER client is still evaluated (never throws)"
          },
          {
            "level": "unit",
            "name": "mcpStatusWith: exactly one McpClientStatus per known client (claude, codex), never omitted"
          },
          {
            "level": "unit",
            "name": "attachedClientsWith: a resolving rpc -> {reachable:true; clients} (sorted); a rejecting rpc -> {reachable:false} without throwing"
          },
          {
            "level": "unit",
            "name": "makeServices().debug exposes attachedClients + mcpStatus bound to the real impls"
          }
        ]
      },
      {
        "id": "t6",
        "title": "Add the MCPSection component + wire it into DebugPane",
        "summary": "Add src/cli/panes/MCPSection.tsx: renders (a) the per-client mcp registration/connection rows from mcpStatus() on mount/refresh (ac1) and (b) the attached-socket-client list from attachedClients() with label + pid + connected-at, polled while open with the interval cleared on unmount (ac2); a { reachable:false } shows a 'daemon unreachable' line while the mcp rows still render; strictly read-only — only `r` refresh + the pane's section-nav, NO disconnect/terminate control (ac3/k5). Swap DebugPane's SECTION_VIEWS mcp entry from the placeholder to <MCPSection services={{ debug }} />.",
        "size": "L",
        "order": 6,
        "dependsOn": [
          "t5"
        ],
        "acceptanceChecks": [
          "MCPSection renders per-client registered + connected rows (independently) from mcpStatus() (ac1)",
          "MCPSection renders the attachedClients() list with label + pid + connected-at and polls while open; the poll interval is cleared on unmount (ac2)",
          "A { reachable:false } attachedClients() renders a 'daemon unreachable' line while the mcp rows still show; empty non-self set → 'no other sessions attached'",
          "No keypress disconnects/terminates a client — only `r` refresh + section-nav (ac3/k5); DebugPane's mcp section maps to <MCPSection/> (placeholder gone)",
          "tsc compiles"
        ],
        "derivedFrom": [
          "c9",
          "c10"
        ],
        "tests": [
          {
            "level": "unit",
            "name": "MCPSection: renders per-client rows from mcpStatus() showing registered + connected independently; registered-but-not-connected case"
          },
          {
            "level": "unit",
            "name": "MCPSection: renders the attachedClients() list with label + pid + connected-at; empty non-self set -> 'no other sessions attached'"
          },
          {
            "level": "unit",
            "name": "MCPSection: a {reachable:false} attachedClients() renders 'daemon unreachable' while the mcp rows still show"
          },
          {
            "level": "unit",
            "name": "MCPSection: no keypress disconnects/terminates a client; only `r` refresh + section-nav honoured (ac3/k5)"
          },
          {
            "level": "unit",
            "name": "MCPSection: the poll interval is cleared on unmount (no post-unmount setState)"
          },
          {
            "level": "integration",
            "name": "DebugPane 'mcp' section maps to <MCPSection/> (the s1 placeholder is gone)"
          }
        ]
      },
      {
        "id": "t7",
        "title": "Update the in-repo debug-facade test fakes for attachedClients/mcpStatus",
        "summary": "Add conformant attachedClients + mcpStatus stubs to every in-repo `debug` facade fake so the widened DebugService compiles: fakeServices() in tui.test.ts, the ctxWith base in command.test.ts, and the props built in DebugPane.test.ts + DaemonSection.test.ts (defaults attachedClients → {reachable:false}, mcpStatus → []). Build note: apply together-with t5 in ONE increment (mirrors s2/s3 t5); dependsOn stays [t5] (the true compile dependency).",
        "size": "S",
        "order": 7,
        "dependsOn": [
          "t5"
        ],
        "acceptanceChecks": [
          "Every in-repo debug-facade fake includes attachedClients + mcpStatus stubs returning valid DebugStatusModel / McpClientStatus[]",
          "`npx tsx --test 'src/cli/**/*.test.ts'` compiles against the widened DebugService",
          "No existing assertion behaviour changes beyond the additive stubs"
        ],
        "derivedFrom": [
          "c6"
        ],
        "tests": [
          {
            "level": "integration",
            "name": "Every in-repo debug facade fake (tui/command/DebugPane/DaemonSection/MCPSection) carries attachedClients + mcpStatus stubs so the widened DebugService compiles"
          }
        ]
      }
    ],
    "testStrategyCoverage": [
      {
        "lldStrategyItem": "IpcServer registry: a new connection creates an entry with a monotonic id + connectedAt from the injected clock; attachedClients() returns it labelled 'unknown' until a message identifies it",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "IpcServer registry: a handleMessage carrying request.client={label,pid} + method updates that connection's label/pid/lastMethod; attachedClients() reflects it",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "IpcServer registry: a socket 'close' deletes the entry; a double 'close' / close-before-message is an idempotent no-op (no throw, no ghost entry)",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "IpcServer registry: a malformed request.client (missing/non-integer pid, empty/oversized label) is ignored — the connection stays 'unknown'/pid undefined (untrusted wire input never stored)",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "attachedClients() returns entries sorted by connectedAt and never mutates/closes any connection (k5 — no disconnect counterpart)",
        "coveredByTaskIds": [
          "t2"
        ]
      },
      {
        "lldStrategyItem": "mcpStatusWith: canned claude+codex `mcp list` output with an `insrc` line + a connection-state token -> per client { registered:true, connected:<parsed>, available:true }",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "mcpStatusWith: stdout with NO insrc line -> { registered:false, connected:false, available:true }",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "mcpStatusWith: a runner spawnError / non-zero exit for one client -> { available:false, registered:false, connected:false } while the OTHER client is still evaluated (never throws)",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "mcpStatusWith: exactly one McpClientStatus per known client (claude, codex) is always returned, never omitted",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "attachedClientsWith: a resolving rpc -> { reachable:true; clients } (sorted); a rejecting rpc (daemon down) -> { reachable:false } without throwing",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "makeServices().debug exposes attachedClients + mcpStatus bound to the real impls",
        "coveredByTaskIds": [
          "t5"
        ]
      },
      {
        "lldStrategyItem": "MCPSection: renders per-client rows from mcpStatus() showing registered + connected independently (ac1); a registered-but-not-connected client renders registered:true/connected:false",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "MCPSection: renders the attachedClients() list with label + pid + connected-at (ac2); empty non-self set -> 'no other sessions attached'",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "MCPSection: a { reachable:false } attachedClients() renders a 'daemon unreachable' line while the mcp-registration rows still show (daemon-independent)",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "MCPSection: no keypress disconnects/terminates a client; only `r` refresh + the pane's section-nav are honoured (ac3/k5)",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "MCPSection: the poll interval is cleared on unmount (no post-unmount setState)",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "Every in-repo `debug` facade fake (tui/command/DebugPane/DaemonSection/MCPSection tests) carries attachedClients + mcpStatus stubs so the widened DebugService compiles",
        "coveredByTaskIds": [
          "t7"
        ]
      },
      {
        "lldStrategyItem": "The DebugPane 'mcp' section maps to <MCPSection/> (the s1 placeholder is gone)",
        "coveredByTaskIds": [
          "t6"
        ]
      },
      {
        "lldStrategyItem": "IpcRequest.client is optional/additive: an existing rpc call with no client envelope still type-checks and the daemon handler map is unchanged apart from the added daemon.debug-status peer",
        "coveredByTaskIds": [
          "t1"
        ]
      }
    ]
  },
  "citations": [
    {
      "id": "c1",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: IpcRequest.client optional identity envelope (additive field-add on the shared wire type, types.ts:749)"
    },
    {
      "id": "c2",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: new types AttachedClient (shared/types.ts) + DebugStatusModel/McpClientStatus (debug-types.ts)"
    },
    {
      "id": "c3",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: IpcServer in-memory connection registry (Map<Socket,ConnInfo> + connSeq + injected now + attachedClients()) maintained across connect/message/close (server.ts)"
    },
    {
      "id": "c4",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: read-only daemon.debug-status RpcHandler returning the attachedClients() snapshot (index.ts handler map, k1)"
    },
    {
      "id": "c5",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: rpc() (client.ts) + the insrc-mcp server populate the IpcRequest.client envelope (label 'cli' / 'mcp:<client>')"
    },
    {
      "id": "c6",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: DebugService facade widening with attachedClients + mcpStatus (interface + impls + makeServices binding; s2/s3 flat-facade extension model; requires the in-repo fakes to stub the new methods)"
    },
    {
      "id": "c7",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: mcpStatus() client-side `mcp list` parse per claude/codex (registered + connected independent, never throws, one entry per client; reuses mcp-register.ts runCli/alreadyRegistered)"
    },
    {
      "id": "c8",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: attachedClients() reads daemon.debug-status over IPC and folds to a discriminated reachable/unreachable DebugStatusModel (never throws)"
    },
    {
      "id": "c9",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: MCPSection component — mcp-registration rows (ac1) + attached-client list polled while open (ac2), strictly read-only with poll-cleanup on unmount (ac3/k5)"
    },
    {
      "id": "c10",
      "kind": "prior-artifact",
      "ref": "LLD s4 handoff: DebugPane SECTION_VIEWS swaps the 'mcp' placeholder for <MCPSection/> (no change to the hosting shape/DebugSectionId/navigation, sc1/s1-owned)"
    }
  ]
}
