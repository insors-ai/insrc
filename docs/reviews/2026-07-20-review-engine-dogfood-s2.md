### ⛔ Review `BLOCK` — plan (plan)

**5 HIGH · 19 MED · 10 LOW** · model `cli-claude` · reviewed 2026-07-20T00:00:00.000Z

| Ref | Kind | Severity | Fixability | Premise | Evidence | Action |
| --- | --- | --- | --- | --- | --- | --- |
| s2/t7 | semantic | HIGH | assisted | Every progress frame emitted on the wire carries the literal id: 0, so frames cannot be correlated to an invocation from their contents and correlation must come from per-invocation lexical closure. | The cited anchor resolves exactly: `src/daemon/workflow-rpc.ts:238` reads `onProgress: (f) => send({ id: 0, stream: 'progress', data: f }),` — so the workflow.run path does hardcode `id: 0`, as do its sibling frames at workflow-rpc.ts:213/218/239/248/250, plus analyze-rpc.ts:493/503/521/535/545/566/567, todos-rpc.ts:1152/1161 and index.ts:101/1439/1441.  But the premise's universal quantifier ("Every progress frame emitted on the wire") is falsified by the second probe. Of the 16 `stream: 'progress'` send sites, 13 carry a real per-invocation id, not 0: `deps.send({ id: deps.requestId, stream: 'progress', ... })` at cloud/aws/logs.ts:112, http/index.ts:396, k8s/index.ts:509/522/778/781, shell/exec-detached.ts:120/126, ssh/index.ts:240/243/506, and test/index.ts:450/453. Only 3 progress sites use `id: 0` (workflow-rpc.ts:238, analyze-rpc.ts:521, index.ts:1439).  This is material, not cosmetic: the premise's conclusion — "correlation must come from per-invocation lexical closure" — is presented as forced by the wire format, yet the same `send` channel demonstrably already carries a per-invocation correlation id in the tool-executor path. `id: 0` is a convention of the three long-lived streaming RPCs, not a constraint of the transport, so "must" does not follow from the evidence. | Narrow the premise's scope to the streaming RPC handlers it actually describes, and stop deriving the closure requirement from the wire format. Suggested restatement: "Progress frames emitted by the streaming RPC handlers (workflow-rpc.ts:238, analyze-rpc.ts:521, index.ts:1439) hardcode `id: 0`, so within workflow.run a frame cannot be correlated to an invocation from its contents; correlation therefore comes from per-invocation lexical closure. Note the tool-executor path already threads `deps.requestId` through the same `send` channel (e.g. shell/exec-detached.ts:120, k8s/index.ts:509), so an id-carrying frame is available if closure proves insufficient." If the design's later steps lean on "correlation is impossible from frame contents," re-check them against the fact that a requestId-carrying alternative exists. |
| s2/t11 | inventory | HIGH | manual | All eight daemon-side progress emission sites are stage-shaped today, so the token variant has no producer and its branch ships dormant. | All three probes contradict the premise's inventory. (1) `/onProgress\\(/` re-derives exactly **one** call site repo-wide — `src/cli/services/setup.ts:101: if (trimmed.length > 0) onProgress({ model, line: trimmed });` — which is in `src/cli`, **not** `src/daemon`, so the premise's "eight daemon-side progress emission sites" has zero members under its own anchor path. (2) `/kind:\\s*'stage'/` re-derives **0** matches, so nothing in the tree is literally "stage-shaped"; the one real emission payload is `{ model, line }`, a line/token-shaped frame, not a stage frame. (3) `/emitProgress\|reportProgress/` re-derives **0** matches, ruling out an alternately-named daemon emitter that the first probe could have missed. The premise's conclusion ("the token variant has no producer and its branch ships dormant") therefore rests on an inventory the evidence inverts: the sole producer found emits per-line text, which is the token-ish variant, and no stage producer exists at all. | Do not accept this premise as written — both its count (eight), its location (`src/daemon`), and its shape claim (`kind: 'stage'`) are unsupported by the re-derived evidence, and the dormancy conclusion it licenses is likely backwards. Re-scope the premise against the real emission surface before any downstream step depends on it: decide whether the plan targets `src/cli/services/setup.ts:101` (the only extant producer) or intends to introduce daemon-side emission that does not yet exist, then restate the inventory with the actual sites and their actual payload shape. No mechanical edit is safe here because the evidence supplies no replacement count or shape to substitute. |
| s2/t1 | citation | HIGH | assisted | buildInsrcMcpServer and runInsrcMcpStdio exist as real symbols in src/mcp/ but their parameter lists and the shape of BuildInsrcMcpServerOpts are unverified in the LLD ('UNVERIFIED SHAPE' placeholder). | The two function anchors resolve, but the grep lines carry their full signatures — the shapes are not actually unverifiable, and the third anchor does not exist at all.  - `src/mcp/server.ts:113: export function buildInsrcMcpServer(): McpServer {` — declared with an **empty parameter list**, returning `McpServer`. Confirmed by its only call site, `src/mcp/server.ts:503: const server = buildInsrcMcpServer();`, which passes no arguments. - `src/mcp/server.ts:481: export async function runInsrcMcpStdio(): Promise<void> {` — also **zero parameters**. Confirmed by `src/bin/insrc-mcp.ts:45: await runInsrcMcpStdio();`, called with no arguments. - `grep /BuildInsrcMcpServerOpts/ → 0 matches` across the source tree. The type is not declared, imported, or referenced anywhere. It is not an unverified shape — it is a nonexistent symbol.  The premise's "UNVERIFIED SHAPE" placeholder is therefore wrong on both counts: the parameter lists are fully determined by the evidence (both empty), and `BuildInsrcMcpServerOpts` is not a real type in `src/mcp/`. Any LLD step that plans to construct, extend, or thread a `BuildInsrcMcpServerOpts` value into `buildInsrcMcpServer()` targets a producer that does not exist and a parameter slot that is not there — that changes the artifact's implementation surface and its acceptance criteria. | Replace the "UNVERIFIED SHAPE" placeholder with the resolved signatures: `buildInsrcMcpServer(): McpServer` and `runInsrcMcpStdio(): Promise<void>`, both taking no parameters. Drop `BuildInsrcMcpServerOpts` from the anchor list — it does not exist. If the LLD's design intent is to make the server configurable, that must be restated as *introducing* a new options type and widening `buildInsrcMcpServer`'s (currently empty) parameter list, not as documenting an existing one; that reframing is a design change and should be flagged for the author rather than silently patched. |
| s2/t1 | inventory | HIGH | assisted | The directory src/mcp/workflow-step/__tests__ exists but its contents were never enumerated, so the plan's 'no test file to extend' premise is unchecked. | The re-run grep for `/workflow-step/__tests__/` returns 12 matches, 11 of which are distinct test files living inside `src/mcp/workflow-step/__tests__/`: `amendments-e2e.test.ts:22`, `define-e2e.test.ts:18`, `define-extend-e2e.test.ts:19`, `design-epic-e2e.test.ts:18`, `design-story-e2e.test.ts:23`, `handler.test.ts:13`, `plan-e2e.test.ts:16`, `questions-gate-e2e.test.ts:25`, `state-store.test.ts:10`, `tracker-e2e.test.ts:23`, `tracker-tasks-coarse.test.ts:15`. The 12th match (`src/workflow/__tests__/scope-extend.test.ts:11`) is an external cross-reference to `define-extend-e2e.test.ts`, not a member of the directory. The `read src/mcp/workflow-step/__tests__:1 → NOT FOUND` result is a read of a directory path and carries no signal about existence; the grep hits establish that the directory is present and populated.  This directly falsifies the plan's "no test file to extend" premise the inventory item flagged as unchecked: the directory is not empty, and it contains at least two files whose names indicate they already cover the surface in question — `handler.test.ts` (the workflow-step handler itself) and `plan-e2e.test.ts` (the `plan` workflow end-to-end). A plan step that creates a new test file on the assumption that none exists would duplicate coverage rather than extend it. | Replace the plan's "no test file to extend" premise with the enumerated inventory above, then re-scope the test task to extend an existing file instead of creating a new one. A human must choose the extension target — the evidence establishes which files exist but not which one owns the behavior under test. |
| s2/t7 | citation | HIGH | assisted | The workflow-step wiring spans exactly two files: the request entry handleWorkflowStep in src/mcp/workflow-step/handler.ts where _meta first becomes observable, and the per-phase dispatch point handleStep in src/mcp/workflow-step/phases/step.ts. | The grep for `handleWorkflowStep` re-derives a third production file the premise omits: `src/mcp/server.ts:46` (`import { handleWorkflowStep } from './workflow-step/handler.js';`) and `src/mcp/server.ts:419` (`async (rawArgs, _extra) => handleWorkflowStep(rawArgs),`). Every other match outside the two named files is a test (`src/mcp/workflow-step/__tests__/*-e2e.test.ts`), so server.ts is the only additional non-test site — but it is the tool-registration site, and it is load-bearing for the premise's specific claim about `_meta`: the MCP callback signature receives `_extra` and discards it, forwarding only `rawArgs` to `handleWorkflowStep`. `_meta` therefore cannot "first become observable" in `handler.ts` unless it is carried inside `rawArgs`; if it arrives via the MCP extra argument, it is dropped at `server.ts:419` before handler.ts ever runs. The second anchor is confirmed: `grep /function handleStep/` → exactly 1 match, `src/mcp/workflow-step/phases/step.ts:30: export async function handleStep(`. The two `read …:1` probes returned only the license-header line (`/*------…`), so they confirm file existence but do not independently confirm either symbol's location; `handleWorkflowStep`'s residence in `handler.ts` is corroborated instead by the import at `src/mcp/server.ts:46`. Note the grep was TRUNCATED at a 50-match cap, so additional production references cannot be ruled out from this evidence. | Correct the file count and re-anchor the `_meta` claim: the wiring spans three production files, adding `src/mcp/server.ts` (import at :46, tool-callback registration at :419) as the true request entry. Then resolve whether `_meta` reaches `handleWorkflowStep` inside `rawArgs` or via the discarded `_extra` parameter — if the latter, the plan must plumb `_extra` through `server.ts:419` first, and any step scoped to "exactly two files" will not compile the feature end-to-end. Re-run the `handleWorkflowStep` grep with the match cap raised (or restricted to non-test paths) to confirm no fourth production site exists. |
| s2/t1 | inventory | MED | assisted | There are exactly three long-running MCP tools — workflow-step, analyze-step, and build-step — each registered inside buildInsrcMcpServer. | The evidence establishes that the three named step tools exist as source trees — `src/mcp/analyze-step/handler.ts:7` ("Top-level `insrc_analyze_step` handler"), `src/mcp/build-step/handler.ts:7` ("Top-level dispatcher for `insrc_build_step`"), and `insrc_workflow_step` referenced from `src/daemon/workflow-rpc.ts:10` and `src/mcp/build-step/phases/implement.ts:15`. It does NOT establish either load-bearing half of the premise. (1) The `buildInsrcMcpServer` grep returned only two hits — its declaration at `src/mcp/server.ts:113` and a call site at `src/mcp/server.ts:503` — and no body lines were read, so nothing shows which tools are registered inside it. Notably, `src/mcp/workflow-step` (an anchor) produced zero direct path matches; every `insrc_workflow_step` hit is in `src/daemon/` or `src/mcp/build-step/`, so even the anchor path is unconfirmed. (2) The `registerTool\|server\\.tool\\(\|setRequestHandler` grep hit its 50-match cap entirely inside `src/daemon/tools/builtins/cloud/{aws,az,gcp}/index.ts` — the daemon's ~110-tool capability registry, an unrelated registrar. Not one line of that grep touches `src/mcp/`, so it provides no evidence for or against the registration claim or the exhaustive count of three. `insrc_analyze` (one-shot) also appears in the regex alternation and is a plausible fourth registered tool the truncated evidence can neither include nor exclude. | Re-run the probe scoped to the registration site rather than the whole tree: read the body of `buildInsrcMcpServer` at `src/mcp/server.ts:113` through its return, and enumerate the actual registration calls (the daemon's `registerTool` is a different API and should be excluded from the pattern — scope the grep to `src/mcp/`). Then re-derive both the count and the member list from that enumeration, and confirm whether the correct anchor for the workflow tool is `src/mcp/workflow-step` or a `src/daemon/workflow-rpc.ts`-backed path, since the evidence only supports the latter. |
| s2/t1 | external-contract | MED | auto | src/daemon/analyze-rpc.ts exists and whether its analyze.run frames reach the wire through the same stream:'progress' binding as workflow-rpc.ts is unverified — t8's rollout narrows to workflow.run only if it does not. | The premise's open question is answered by the gathered evidence, and it resolves against the branch t8 hedges toward. (1) The file exists and is substantial: src/daemon/analyze-rpc.ts:186 documents "analyze.run.start: full end-to-end pipeline", :451 is the handler, :703 analyze.run.status, :745 analyze.run.purge. (2) Its progress frames reach the wire through the SAME binding as workflow-rpc.ts: analyze-rpc.ts:521 `send({ id: 0, stream: 'progress', data: eventToProgressData(event) })` vs workflow-rpc.ts:238 `onProgress: (f) => send({ id: 0, stream: 'progress', data: f })` — identical `id: 0` and `stream: 'progress'` sentinel; only the data shaping differs. (3) analyze-rpc.ts:462 documents the contract inline as `{ stream: 'progress', data: { step, status, ...event-specific } }`. (4) It is wired into the daemon with a `send` argument: src/daemon/index.ts:1415 `'analyze.run.start': async (params, send, signal) => {`, so streaming genuinely reaches the transport. (5) It is test-covered: src/daemon/__tests__/analyze-rpc.test.ts:462 "analyze.run.start -- streaming params validation" and analyze-rpc.live.test.ts:425 "end-to-end via the daemon RPC handler". Therefore the conditional "t8's rollout narrows to workflow.run only if it does not [reach the wire through the same binding]" collapses — it DOES, so narrowing is not warranted on those grounds. Not HIGH because the premise asserted an unknown rather than a falsehood; MED because leaving it unresolved lets t8 justify a workflow.run-only scope on a condition the source contradicts. One thing the evidence does NOT settle: whether the two `data` payload shapes are mutually compatible for a shared consumer — analyze-rpc.ts routes events through eventToProgressData(), workflow-rpc.ts forwards the frame unchanged. Shared binding does not imply shared schema. | Rewrite the premise from an open question to the verified finding: src/daemon/analyze-rpc.ts exists and its analyze.run.start frames reach the wire through the same `{ id: 0, stream: 'progress' }` binding as workflow-rpc.ts:238 (analyze-rpc.ts:521), so t8's rollout should NOT narrow to workflow.run on transport-binding grounds. Separately, open a new premise to probe whether the two `data` payload shapes (eventToProgressData(event) vs. the raw workflow frame) are compatible for a single consumer — that is the real remaining risk to a combined rollout. |
| s2/t2 | inventory | MED | manual | src/mcp/workflow-step contains 32 classes, all unanimously PascalCase — refuting the LLD's 'class sample size 0' rationale for that module. | The gathered evidence does not address the premise's anchor at all. The class-declaration grep returned 50 matches and was TRUNCATED at the cap, and every single returned match lies outside `src/mcp/workflow-step`: they come from `src/agent/providers/` (cli-provider.ts:88, mcp-sampling-provider.ts:149, ollama.ts:136), `src/analyze/**` (classifier/driver.ts:67,74,81,94; context/driver.ts:133,140,147,157; planner/driver.ts:72,79,86,104,111; executor/types.ts:159,172; summariser/driver.ts:60; etc.), `src/config/store.ts:106`, and `src/daemon/**` (chat-sessions.ts:52, db/drivers/*.ts, __tests__/workflow-rpc.test.ts:33). The match list is alphabetically ordered and stops mid-`src/daemon/db/drivers/` (nats.ts:37), i.e. it was cut off well before `src/mcp/` would have been reached. So the evidence neither confirms nor refutes "32 classes" under `src/mcp/workflow-step`, and it provides zero observations bearing on the "all unanimously PascalCase" claim for that module — and therefore cannot support the premise's stronger conclusion that the LLD's "class sample size 0" rationale is refuted. Notably, the one incidental datapoint about naming discipline in the returned set is uniform PascalCase, but from unrelated modules, so it carries no weight for the anchored directory. | Re-run the probe scoped to the anchor before accepting or rejecting the premise: restrict the class-declaration grep to `src/mcp/workflow-step` (and raise or remove the 50-match cap) so the count and the casing claim are derived from that directory alone. If the scoped re-run returns zero classes, the premise is HIGH-severity wrong and the LLD's "class sample size 0" rationale stands; if it returns a count other than 32, correct the number in the artifact from the re-derived evidence. Until a scoped probe exists, this premise should not be treated as load-bearing. |
| s2/t2 | citation | MED | auto | src/mcp/ uses kebab-case filenames with state-store.ts and questions-gate.ts as the attested sibling examples the new sink filename must follow. | Both cited anchors fail to resolve: `read src/mcp/state-store.ts:1 → NOT FOUND` and `read src/mcp/questions-gate.ts:1 → NOT FOUND`. No file exists at either path. The real files live one directory deeper — the grep re-derives `src/mcp/analyze-step/state-store.ts:47` (`getLogger('mcp:analyze-step:state-store')`), `src/mcp/workflow-step/state-store.ts:25` (`getLogger('mcp:workflow-step:state-store')`), and `src/mcp/workflow-step/questions-gate.ts:34` (`getLogger('mcp:workflow-step:questions-gate')`). Import sites confirm the nesting: `src/mcp/analyze-step/state.ts:40` imports `'./state-store.js'`, `src/mcp/analyze-step/phases/bundle.ts:38` imports `'../state-store.js'`, and `src/mcp/workflow-step/phases/start.ts:30` imports `'../questions-gate.js'` — all relative-within-subdirectory, never from an `src/mcp/` root module. The premise's substantive claim is nonetheless upheld by the same evidence: every filename surfaced under `src/mcp/` is kebab-case (`state-store.ts`, `questions-gate.ts`, `resolve-question.ts`, `review-deferred.ts`, `analyze-step-state.test.ts`, `tracker-tasks-coarse.test.ts`, `define-extend-e2e.test.ts`). Rated MED rather than HIGH because the kebab-case convention the premise depends on is verified — only the anchor paths, and the implied "sibling" (same-directory) relationship, are wrong. | Correct both anchors to their real paths — `src/mcp/analyze-step/state-store.ts` and `src/mcp/workflow-step/questions-gate.ts` — and drop the "sibling" framing, since neither file sits at the `src/mcp/` root. If the artifact relies on these anchors to fix the new sink file's *directory* (not just its filename casing), that placement decision is unresolved by this evidence and needs to be settled separately: the two exemplars live in different subdirectories (`analyze-step/` vs `workflow-step/`), so they attest a naming convention but not a location. |
| s2/t3 | closed-union | MED | manual | The sc1 ProgressEvent union has exactly two variants discriminated on event.kind — 'stage' and 'token' — and report() branches on exactly those two plus a default arm. | The probes do not confirm the premise anywhere in the real source tree. `grep /type ProgressEvent/` returned **0 matches** — there is no `ProgressEvent` type declaration to inspect, so the claim that the union "has exactly two variants" has no declaration to be checked against. `grep /kind:\\s*'(stage\|token)'/` returned **0 matches** — neither the `'stage'` nor the `'token'` discriminant literal appears in any file, so the two named variants and the `report()` branch arms (two + default) are entirely unattested. The only 5 hits for `(Stage\|Token)ProgressEvent` are all inside a single file, `src/workflow/review/__tests__/review.test.ts` (lines 52, 76, 77, 78, 115), and every one of them is a **string literal inside a test fixture for the review framework itself** — e.g. line 52 `'- t3: TokenProgressEvent holds the token count.'`, line 77 `anchors: ['TokenProgressEvent']`, line 78 `probe: { greps: ['TokenProgressEvent'] }`. These are quoted premise/anchor/probe fixtures being fed to the reviewer under test, not a type definition, not an import, and not a `report()` implementation. No `StageProgressEvent` declaration, no `TokenProgressEvent` declaration, and no `report()` switch were read. The evidence is therefore self-referential to the review harness and does not address the premise: the premise is UNVERIFIABLE, not disproven. | Do not accept `s2/t3` as grounded. Establish whether `ProgressEvent` is (a) prospective — a type the HLD proposes to introduce, in which case the premise must be restated in prospective voice and its probes deferred until the implementing change lands, or (b) a stale/renamed reference to a progress type that exists under different naming, in which case re-anchor the premise to the real declaration. Re-run the probes against the actual declaration site once it exists; a closed-union premise asserting an exact variant count must cite the `file:line` of the union declaration and of each `report()` branch arm. Separately, exclude `src/workflow/review/__tests__/review.test.ts` (or `__tests__` fixtures generally) from probe result sets — the harness's own fixture strings echo any anchor back and manufacture false grounding. |
| s2/t3 | semantic | MED | manual | The sc1 stage variant carries index, total (number or null), and stageLabel; the token variant carries tokensTotal and tokensDelta — these field names are the exact source of params.progress / params.total / params.message. | Every probe attached to this premise returned zero matches against the real source tree: grep /stageLabel/ → 0 matches; grep /tokensTotal/ → 0 matches; grep /tokensDelta/ → 0 matches; grep /total:\\s*number\\s*\\\|\\s*null/ → 0 matches. No `path:line` reads were returned for the named anchors `StageProgressEvent` or `TokenProgressEvent` either. The premise asserts a concrete field-level contract — that the sc1 stage variant carries `index`, `total (number\|null)`, `stageLabel`, and the token variant carries `tokensTotal`/`tokensDelta`, and that these are "the exact source of" `params.progress` / `params.total` / `params.message`. The gathered evidence contains no occurrence of any of those three distinctive field names, and no match for the `total: number \| null` shape. So the evidence neither confirms the field names nor confirms the mapping onto `params.*`. It is equally consistent with (a) the premise describing types that do not yet exist (a forward-looking design premise), (b) the field names being wrong/renamed in the actual source, or (c) the probes being scoped to a path where these types do not live. Nothing in the handed evidence lets me distinguish those cases, so I do not treat the zero-match result as proof the producers are absent — only as absence of grounding. | Re-ground this premise before it is relied on for acceptance. Either (1) re-run the probes unscoped across the tree (and against the actual declaration sites of `StageProgressEvent` / `TokenProgressEvent`) to produce real `path:line` reads for each asserted field, or (2) if these types are being introduced by this plan rather than read from existing code, restate the premise as a to-be-built contract rather than as an assertion about the current source, so it is not scored as a verifiable claim about existing producers. Do not accept the `params.progress` / `params.total` / `params.message` mapping as verified until a read shows the mapping site. |
| s2/t10 | closed-union | MED | manual | sc1 declares ProgressOperation as exactly the two-member union 'workflow.run' \| 'analyze.run'. | The probes do not confirm the premise. `grep /ProgressOperation/` returned 0 matches — the type the premise says sc1 declares is absent from the source tree entirely, so its arity ("exactly the two-member union") cannot be checked against anything. Of the two claimed members, only one has any referent: `grep /'workflow\\.run'/` returned a single match at src/daemon/index.ts:1423 (`'workflow.run': async (params, send, signal) => {`), which is an IPC handler key in the daemon's handler registry, not a member of a declared union type. `grep /'analyze\\.run'/` returned 0 matches — no such operation string exists in the tree at all. So the evidence neither confirms the union's membership nor refutes it; it establishes only that the declaration is not present at review time and that one of the two named operations has no producer today. | Re-ground the premise before it is treated as load-bearing. Either (a) point the anchor at the file/line where sc1 is expected to declare `ProgressOperation` once that artifact lands and re-run the probe, or (b) if sc1 is a forward-looking spec that has not been implemented, mark the premise as prospective rather than a closed-union assertion over existing code. Separately, resolve whether `analyze.run` is a planned operation or a stale name: only `workflow.run` exists as an IPC handler (src/daemon/index.ts:1423), so a two-member union naming `analyze.run` presumes a producer that is not in the tree. |
| s2/t1 | inventory | MED | manual | The LLD s2 invariantsToPreserve list has exactly nine entries, of which five — numbers 2, 3, 4, 5 and 8 — are grounded only in HLD text and the alternative judgment rather than in a bundle showing the invariant holds in code. | The premise is a claim about the *contents* of the LLD s2 `invariantsToPreserve` list — a count of nine entries and the grounding quality of five specific members (2, 3, 4, 5, 8). None of the gathered evidence addresses that.  The 17 ripgrep matches for `/invariantsToPreserve/` all resolve to the *type and schema definitions* plus *test fixtures*, never to the LLD artifact under review: - Schema/type declarations: `src/workflow/artifacts/lld.ts:86` (`readonly invariantsToPreserve: readonly {`), `src/workflow/artifacts/lld.ts:347` and `:350` (render loop), `src/workflow/runners/design-story/schemas.ts:255` and `:290`. - Grounding *rules* rather than grounding *data*: `src/workflow/runners/design-story/index.ts:336` ("invariantsToPreserve must cite an analyze bundle from s1 showing the invariant" for `flavor="enhancement"`) and `:492` ("ep3: enhancement — every `invariantsToPreserve` cites an analyze bundle showing the invariant"). These confirm the *policy* the premise invokes but say nothing about whether this LLD's entries satisfy it. - Every remaining match is a test fixture with a list of length 0 or 1 — e.g. `src/workflow/__tests__/chain.test.ts:94` and `src/workflow/__tests__/plan-gate.test.ts:88` (`invariantsToPreserve: []`), `src/workflow/__tests__/lld-artifact.test.ts:100` and `src/mcp/workflow-step/__tests__/design-story-e2e.test.ts:172` (one entry, `source: 'c1'`). No fixture has nine entries, so none corroborates the count.  The only probe aimed at artifact content, `read .insrc/artifacts/HLD-6d6cfaf9a9b14bd4.json:1`, returned a single character — `{` — the opening brace of the JSON document. It carries no `invariantsToPreserve` payload. It is also pointed at an **HLD** artifact, whereas the premise concerns the **LLD** s2 body, so even a full read of that file would be the wrong document.  Net: the count of nine is neither confirmed nor refuted, and the grounding claim about entries 2, 3, 4, 5 and 8 is entirely untested — no evidence shows which entries cite an s1 analyze bundle versus HLD text or the alternative judgment. | Re-run the premise's probes against the actual LLD artifact before accepting or rejecting it. Concretely: (1) locate the LLD JSON under `.insrc/artifacts/` (the probe read an `HLD-*.json`, not an LLD), (2) read its full `body.errorPaths.invariantsToPreserve` array rather than byte 1 of the file, (3) count the entries to verify "exactly nine", and (4) inspect each entry's `source` field to determine which cite an s1 analyze bundle and which cite HLD text or the alternative judgment — that is what decides whether entries 2, 3, 4, 5 and 8 are the correct set. The rule the premise leans on is real and is stated at `src/workflow/runners/design-story/index.ts:336` and `:492`, so the check is well-defined once the right artifact is read. Do not auto-edit the premise on the current evidence — a corrected count or member list would be invented, not derived. |
| s2 | cross-artifact | MED | manual | The LLD s2 testStrategy contains exactly twelve strategy items, and the plan's Test-strategy coverage table maps all twelve to Tasks with no item left uncovered. | The premise asserts a specific cardinality ("exactly twelve strategy items") and a total-coverage mapping in the plan's table, but neither anchor was actually probed. The `/testStrategy/` grep returned 50 matches TRUNCATED AT CAP, all of which are source/test code rather than artifact content: the type declaration `src/workflow/artifacts/lld.ts:130` (`readonly testStrategy: TestStrategy`), the markdown renderer `lld.ts:358-375`, the shape guard `lld.ts:446`, the acceptance-criterion validator `lld.ts:507-521`, the plan-side coverage type `src/workflow/artifacts/plan.ts:45` (`readonly lldStrategyItem: string; // a verbatim item from the LLD testStrategy`) and `plan.ts:81,135-160`, the orchestrator's verbatim-carry rules `orchestrator.ts:1120` ("`body.testStrategy` MUST be verbatim from s6"), `orchestrator.ts:1451` ("`body.testStrategyCoverage` MUST be the s5 testStrategyCoverage verbatim"), the coverage check `orchestrator.ts:1528` (`checkTestStrategyCoverage(body.tasks, body.testStrategyCoverage, lld.body.testStrategy)`), and the runner/schema `runners/design-story/index.ts:368,407` + `schemas.ts:309`. Every count-bearing hit is a TEST FIXTURE with one or zero items — `plan-e2e.test.ts:90` (one testLevel, one subject), `plan-e2e.test.ts:110` (one coverage row), `plan-artifact.test.ts:180` (one row), `chain.test.ts:95` and `questions.test.ts:74` (empty arrays), `tracker-tasks-coarse.test.ts:72` (`testStrategyCoverage: []`) — none of which is the s2 artifact body. The only artifact read, `.insrc/artifacts/HLD-6d6cfaf9a9b14bd4.json:1`, returned the single character `{`; it is additionally an HLD, not the LLD s2 body the premise names. The evidence therefore contains no enumeration of s2's testStrategy items and no rendering of the plan's Test-strategy coverage table, so the number twelve is neither confirmed nor refuted, and the "no item left uncovered" claim is untested. The presence of `checkTestStrategyCoverage` at `orchestrator.ts:1528` shows a coverage gate exists, but a passing gate would only prove coverage is non-empty and criterion-resolvable — not that the inventory is exactly twelve nor that all twelve map to Tasks. | Re-run the premise's probes against the actual artifact bodies rather than the source tree: read the LLD s2 artifact JSON and enumerate `body.testStrategy.testLevels[*].subjects` (plus `acceptanceMapping[*].provingTests`, since the plan's `lldStrategyItem` is documented at plan.ts:45 as a verbatim item from the LLD testStrategy) to derive the true item count; then read the plan artifact's `body.testStrategyCoverage` and confirm every derived item appears with a non-empty `coveredByTaskIds`. Correct the "twelve" figure in the premise to the re-derived count and flag any item with no covering Task. Note the anchor is also mis-targeted — the probe read an HLD file (`HLD-6d6cfaf9a9b14bd4.json`) for an LLD-scoped premise — so fix the anchor to point at the LLD and plan artifacts before re-judging. Do not apply any textual edit to the artifact until the real count is in hand. |
| s2 | inventory | MED | manual | The plan enumerates exactly eleven Tasks t1 through t11, and every Task's 'Depends on' entry names only Tasks within that set. | The only probe re-run for this premise was `grep /E202607206d6cfaf9:S002:T0[0-1][0-9]/`, which returned **0 matches** against the source tree. No Task-ID occurrences (`T001`–`T011` or otherwise) were recovered, and no `path:line` reads were supplied. There is therefore no evidence that eleven Tasks `t1`–`t11` exist, nor any evidence about the contents of their `Depends on` entries — the anchors `t1`…`t11` are entirely unresolved by the gathered ground truth. | Re-ground the premise with a probe that can actually resolve it. The `E202607206d6cfaf9:S002:T0[0-1][0-9]` pattern matched nothing, so either the ID scheme in the probe is wrong (wrong epic/story prefix, wrong zero-padding, or Tasks are not persisted with fully-qualified IDs at all) or the Tasks live only in the artifact markdown and never in the source tree. Determine where Task records are actually written, fix the probe pattern accordingly, then re-run to (a) count the Tasks and (b) confirm every `Depends on` target is a member of that same set. Do not accept the "exactly eleven, closed dependency set" claim until a probe returns non-empty output. |
| s2 | ordering | MED | manual | The Task dependency graph is acyclic with t1 and t2 as roots: t3←t2; t4←t3; t5←t3; t6←t2; t7←t1,t3,t6; t8←t7; t9←t7,t8; t10←t1,t8; t11←t9,t10. | The only evidence gathered is a ripgrep for `/Depends on/`, which returned 6 matches — and every one of them is a *renderer or test-of-renderer*, not task graph data: - `src/workflow/artifacts/plan.ts:112` — emits the plan table header `\| # \| Task \| Size \| Depends on \| Tests \| Derived from \|` - `src/workflow/artifacts/define.ts:149` — `**Depends on:** ${s.dependsOn...}` (define artifact scopes) - `src/workflow/artifacts/hld.ts:160` — `**Depends on:** ${sb.depends...}` (HLD sub-blocks) - `src/workflow/tracker/conventions.ts:181` — `**Depends on:** ${task.dependsOn...}` (tracker issue body) - `src/workflow/__tests__/plan-artifact.test.ts:193` and `define-artifact.test.ts:95` — assertions that those literals appear  None of these matches contains a task identifier (`t1`…`t11`), an edge, or any instance data. The probe re-derived the *label used to serialize* `dependsOn`, not the `dependsOn` values themselves. So the evidence cannot confirm or refute any of the nine claimed edges (t3←t2; t4←t3; t5←t3; t6←t2; t7←t1,t3,t6; t8←t7; t9←t7,t8; t10←t1,t8; t11←t9,t10), cannot confirm t1 and t2 are the only roots, and cannot confirm acyclicity. It also cannot adjudicate an internal inconsistency visible in the premise itself: the edge list names t4 and t5, but the `anchors` field lists only t1, t2, t3, t6, t7, t8, t9, t10, t11 — t4 and t5 are absent. | Re-run the premise with a probe that reads the actual task inventory rather than the renderer. The `dependsOn` values live in the artifact/plan data, not in `src/workflow/artifacts/plan.ts`; a grep for the string `Depends on` will only ever hit format strings. Ground the ordering claim against the plan artifact's own task rows (the `\| # \| Task \| Size \| Depends on \| ... \|` table body, or the `dependsOn` arrays on the plan's task objects), then verify roots and acyclicity by topological sort over the re-derived edge set. Separately, reconcile the `anchors` list with the edge list — decide whether t4 and t5 are genuinely out of scope for this premise or were dropped by mistake. |
| s2/t5 | citation | MED | assisted | The new sink test file target location is src/mcp/__tests__/, and analyze-step-handler.test.ts is the existing pattern it follows (node:test + node:assert/strict, hand-written doubles, no external mocking library). | Anchor VERIFIED: `read src/mcp/__tests__/analyze-step-handler.test.ts:1 → FOUND: /**` — both the target directory `src/mcp/__tests__/` and the cited exemplar file exist at the stated path, and the file opens with a JSDoc block. The `grep /analyze-step-handler\\.test\\.ts/ → 0 match(es)` result is NOT a contradiction: test files are discovered by glob, never imported by name, so zero in-source references is the expected state for every test file in this repo.  Convention claim PARTIALLY supported: `grep /node:test/ → 50 match(es)` and `grep /node:assert\\/strict/ → 50 match(es)`, both TRUNCATED at cap, establish `import { test } from 'node:test'` + `import assert from 'node:assert/strict'` as the dominant (plausibly universal) test idiom — e.g. `src/analyze/context/__tests__/bundle.test.ts:26/:27`, `src/analyze/orchestrator/__tests__/orchestrator.test.ts:24/:25`, `src/agent/providers/__tests__/mcp-sampling-provider.test.ts:19/:20`.  Gap: both greps truncated alphabetically and never reached `src/mcp/` — every returned line is under `src/agent/` or `src/analyze/`. So the evidence does NOT directly show that `analyze-step-handler.test.ts` itself uses `node:test`/`node:assert/strict`. Its absence from the shown matches is a cap artifact, not a negative result. Separately, the sub-claims "hand-written doubles" and "no external mocking library" have NO probe backing them at all — no grep for a mocking package, `mock.fn`, `t.mock`, or a stub/fake helper was run, and only line 1 of the file was read.  Net: the load-bearing part of the premise (where the new sink test goes, and that a sibling exemplar exists there) is sound. The descriptive characterization of that exemplar's internals is unverified — non-material to the plan's output, since the repo-wide idiom the premise names is correct regardless. | Re-probe with anchors scoped to the file actually being cited before treating its style description as grounded: (1) `rg -n "node:test\|node:assert/strict" src/mcp/__tests__/analyze-step-handler.test.ts` to confirm the two imports in that specific file rather than inferring from the truncated repo-wide sweep; (2) `rg -n "mock\|sinon\|jest\|vi\\.fn\|t\\.mock" src/mcp/__tests__/analyze-step-handler.test.ts` to substantiate or drop the "hand-written doubles, no external mocking library" clause. If (2) returns nothing, the clause is confirmed and the premise becomes LOW as written. If it hits `mock.fn`/`t.mock` (node:test's built-in mocking — still not an *external* library), narrow the wording to "no third-party mocking library" so the premise stays accurate. No artifact edit is safe right now: the evidence contradicts nothing, so any replacement text would be invented rather than derived. |
| s2/t9 | citation | MED | assisted | src/daemon/__tests__/workflow-rpc.test.ts exists and is the reference for stubbing the stream:'progress' / id: 0 frame-source surface. | The evidence confirms only the anchor's *existence*, not its *content claim*. The grep for `/workflow-rpc\\.test\\.ts/` returned exactly 1 match, and it is a self-referential run instruction inside the file itself — `src/daemon/__tests__/workflow-rpc.test.ts:16: * Run: npx tsx --test src/daemon/__tests__/workflow-rpc.test.ts`. The read of `src/daemon/__tests__/workflow-rpc.test.ts:1` returned `/*------...` — a license/banner header comment, which establishes the file is present and readable but says nothing about its subject matter. No probe searched for `stream`, `'progress'`, `id: 0`, or any frame-source stubbing construct, and no gathered line contains any of those tokens. So the load-bearing half of the premise — that this file is *the reference for stubbing the `stream:'progress'` / `id: 0` frame-source surface* — is entirely unaddressed by the evidence handed to me. Note also that the single grep hit being the file's own run-instruction comment means no *other* file in the tree references this test as a pattern source, which is weak counter-evidence against calling it an established "reference". | Re-run the probe set against the anchor with content-bearing patterns before treating this citation as grounded: grep within `src/daemon/__tests__/workflow-rpc.test.ts` for `stream`, `progress`, and `id: 0`, and read the surrounding stub/mock block rather than line 1. If those tokens are present, downgrade to LOW and cite the concrete `path:line`; if absent, the citation points at the wrong file and the premise must name whichever test actually stubs that surface. Do not auto-edit — the evidence supplies no verified replacement referent. |
| s2/t8 | cross-artifact | MED | assisted | analyze-step and build-step have the same two-file shape as workflow-step — a request entry handler plus a per-phase dispatch point — so the t7 helper applies unchanged. | The evidence confirms only half the premise. Entry handlers exist for both targets: `src/mcp/analyze-step/handler.ts:56` (`export async function handleAnalyzeStep(input: unknown): Promise<StepMcpEnvelope>`) and `src/mcp/build-step/handler.ts:23` (`export async function handleBuildStep(input: unknown): Promise<BuildStepMcpEnvelope>`), both wired as single request entry points in `src/mcp/server.ts:300` and `src/mcp/server.ts:471`. Both anchor files were read and resolved (line 1 FOUND in each).  The "per-phase dispatch point" half is NOT confirmed. The only probe touching phase dispatch was `grep /phases/step/`, which returned exactly 1 match — `src/mcp/workflow-step/handler.ts:18: import { handleStep } from './phases/step.js';` — i.e. it re-derives the dispatch point for workflow-step ONLY. No match, read, or listing in the gathered evidence establishes that `src/mcp/analyze-step/phases` or `src/mcp/build-step/phases` (two of the four declared anchors) exist at all. The remaining 28 matches are all `handleAnalyzeStep`/`handleBuildStep` call sites in tests and `server.ts`, plus a comment at `src/mcp/analyze-step/types.ts:150`; none of them touch phase dispatch.  Note the probe was framed as a workflow-step-specific literal (`phases/step`), so its single hit is not evidence that analyze-step/build-step lack a dispatch point — it simply never looked. The claim that "the t7 helper applies unchanged" rests entirely on this unprobed half. | Re-probe the two unverified anchors before relying on s2/t8: list `src/mcp/analyze-step/` and `src/mcp/build-step/` directory contents, and grep both handlers for their internal dispatch imports with a non-workflow-specific pattern (e.g. `from './phases/` or `switch (phase`). If both resolve to a phases-directory shape, downgrade this premise to verified; if either dispatches inline inside `handler.ts` (a plausible shape given `handleBuildStep` sits at line 23, very near the top of its file), the "same two-file shape" claim and the "t7 helper applies unchanged" conclusion both need rework. |
| s2/t2 | external-contract | MED | assisted | The MCP protocol progress notification method is the literal string 'notifications/progress' with params.progressToken and params.progress required and params.total/params.message optional, and the server exposes a notification-sending call that returns a promise. | The gathered evidence does not confirm any part of the premise's protocol-level claims. `grep /notifications\\/progress/` returned **0 matches** across the tree, and `grep /sendNotification\|notification\\(/` also returned **0 matches** — so neither the literal method string nor any notification-sending call exists anywhere in this source tree today. The `McpProgressNotification` anchor likewise has no corresponding hit in the evidence. The only positive evidence is that `@modelcontextprotocol/sdk` is genuinely a dependency, via 4 real imports: `src/mcp/sampling-bridge.ts:26` (`@modelcontextprotocol/sdk/types.js`), `src/mcp/sampling-bridge.ts:27` (`type { Server as McpServerLowLevel } from '@modelcontextprotocol/sdk/server/index.js'`), `src/mcp/server.ts:36` (`McpServer` from `server/mcp.js`), and `src/mcp/server.ts:37` (`StdioServerTransport` from `server/stdio.js`). The `read package.json:1` probe returned only `{` — it confirms the file exists but pins no SDK version, so even the SDK version whose surface the premise describes is unestablished. Notably, the server entry at `src/mcp/server.ts:36` binds the **high-level** `McpServer` facade, while the low-level `Server` type appears only as a type-import in the sampling bridge — the evidence therefore does not establish that the object the plan will call a notification method on exposes one, nor that it returns a promise. | Treat this premise as unverified before building on it. Add an explicit grounding step that resolves the SDK version actually installed (read the `@modelcontextprotocol/sdk` entry in `package.json`/lockfile) and then reads that version's `ServerNotification`/progress-notification type and the notification-sending method off the concrete server object used at `src/mcp/server.ts:36`, confirming (a) the literal method string, (b) which of `progressToken`/`progress`/`total`/`message` are required vs optional, and (c) whether the call is promise-returning. Until that lands, restate the premise as an assumption about an external dependency rather than a verified contract, and do not let acceptance criteria depend on the exact field-optionality wording. |
| s2/t5 | inventory | MED | assisted | package.json declares no external assertion or mocking library, and none is added by t5 — all doubles are hand-written. | The premise asserts a fact about `package.json`'s declared dependencies, but neither probe interrogates that file's dependency blocks.  1. The ripgrep probe `/sinon\|jest\|vitest\|chai\|proxyquire\|testdouble/` returned 50 matches, **all** under `src/` — and every displayed hit is a false positive on substrings or on template payloads, not a dependency declaration:    - `chain` substring hits: `src/cli/services/workflow.ts:125` (`export function chain(...)`), `src/cli/services/index.ts:51-52`, `src/workflow/chain.js` imports, `src/cli/panes/WorkflowsPane.tsx:99`, `src/analyze/planner/templates/bootstrap.ts:10`.    - `keychain` substring hits: `src/daemon/db/secrets.ts:2,5,39,53,75`, `src/daemon/db/pool.ts:14`, `src/daemon/db-rpc.ts:15`, `src/daemon/db/__tests__/secrets.test.ts:7,62`, `src/daemon/db/drivers/dynamodb.ts:20`.    - The only genuine `vitest` token hits are **template content**, not imports or deps: `src/config/templates.ts:19-20` (`name: vitest-unit` / `tags: [unit, vitest, typescript]`), `src/config/frontmatter.ts:10-11` (a docblock example), `src/config/paths.ts:47` (`~/.insrc/templates/tester/vitest-unit.md`), `src/config/templates.ts:107` (`vitest-unit.md` filename selection).    - **Zero matches in `package.json` appear in the returned set** — but the result is explicitly `[TRUNCATED at cap]`, so absence here is not evidence of absence.  2. The read probe `package.json:1` returned only `{` — the opening brace. It never reached `dependencies` / `devDependencies`, so the file's declared libraries were never observed.  Separately, the grep pattern would not detect several common assertion/mocking libraries at all (`expect`, `should`, `nock`, `msw`, `ts-mockito`, `@sinonjs/*`), so even an untruncated result over `package.json` would be a partial check of "no external assertion or mocking library."  The premise may well be true — the visible test files (`src/cli/__tests__/tui.test.ts:70-71`, `src/cli/__tests__/command.test.ts:47-48`) show hand-written object-literal doubles and `svc.workflow.chain = h => {...}` monkey-patching, consistent with node:test + hand-rolled stubs, and `src/daemon/db/__tests__/secrets.test.ts:7` references `mock.method`, the built-in `node:test` mocker. But that is corroborating inference from test bodies, not the declaration-level evidence the premise anchors on. | Re-ground the premise with probes that actually read the declaration site: (a) a read of `package.json` scoped to the `dependencies` and `devDependencies` blocks (not line 1), and (b) a grep anchored to `package.json` rather than the whole tree, widened beyond the current five names to cover `expect\|should\|nock\|msw\|ts-mockito\|@sinonjs\|@types/(sinon\|jest\|chai)`. Until the dependency blocks are observed, the "declares no external assertion or mocking library" clause of s2/t5 cannot be accepted as grounded, and the "none is added by t5" clause has no probe against it at all. Do not auto-edit the artifact text — the premise is unverified, not disproven. |
| s2/t6 | external-contract | MED | assisted | The project compiles under strict with exactOptionalPropertyTypes and noUncheckedIndexedAccess enabled, which is what makes `_meta?: LongRunningToolMeta \| undefined` and the `_meta: {}` optional-read path distinct test cases. | The gathered evidence does not confirm the premise. The only hit for `exactOptionalPropertyTypes` in the tree is a prose comment in application code — `src/workflow/review/extract.ts:161: *  and honouring \\`exactOptionalPropertyTypes\\` (omit rather than \\`undefined\\`). */` — not a compiler-options entry. `noUncheckedIndexedAccess` returned 0 matches and `"strict"` returned 0 matches anywhere in the searched tree. The only read of the cited anchor, `tsconfig.json:1`, resolved to `{` — the opening brace alone, with no compilerOptions body captured. So none of the three anchors the premise leans on (`tsconfig.json`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`) were shown to be actually set in the build config. The evidence is equally consistent with (a) the flags living in an extended base config the probe never opened, or (b) the flags not being enabled at all; it cannot distinguish these. Because the premise is what makes `_meta?: LongRunningToolMeta \| undefined` and the `_meta: {}` optional-read path *distinct* test cases, an unconfirmed strictness setting leaves that test-case split ungrounded — under a non-strict or non-`exactOptionalPropertyTypes` build the two cases collapse into one. | Re-run the probe as a full read of `tsconfig.json` (all lines, not line 1) plus any config it `extends`, and confirm `strict`, `exactOptionalPropertyTypes`, and `noUncheckedIndexedAccess` are literally present and `true`. If they are, re-anchor the premise to the resolved `tsconfig.json:<line>` for each flag. If they are not, the premise must be rewritten and the s2/t6 test-case split re-justified on other grounds (or merged). Do not treat the `extract.ts:161` comment as grounding — it is a code comment about the convention, not evidence the compiler enforces it. |
| s2/t3 | citation | MED | assisted | Emission-fault logging uses getLogger from src/shared/logger.js at warn level for throw/reject and debug level for the unknown-kind arm, consistent with the project's no-console rule. | The evidence confirms only the generic half of the premise. `src/shared/logger.ts:1` reads `import pino from 'pino';`, and the `getLogger` grep returns 50 (capped) call sites all following the same shape — `import { getLogger } from '../../shared/logger.js';` + `const log = getLogger('<module>');` (e.g. `src/agent/providers/cli-provider.ts:52,54`, `src/analyze/executor/walker.ts:40,58`). The `\\.warn\\(\|\\.debug\\(` grep likewise confirms both levels are in live use on those loggers (`src/analyze/executor/walker.ts:231` `log.warn({ runId, taskId, err: msg }, 'leaf task runtime threw')`, `src/analyze/context/cache.ts:147` `log.debug(... 'cache key mismatch; discarding')`). What the evidence does NOT contain is any emission-fault site: no match resolves to a throw/reject arm or an unknown-kind arm of an emitter, and both greps were TRUNCATED at a 50-match cap, so the absence is not even a clean negative. The premise's specific claim — warn for throw/reject, debug for the unknown-kind arm — is unsupported by anything handed to me. | Re-probe with anchors that actually target the emission path rather than the logger module: grep the emitter source file for `log.warn(`/`log.debug(` and cite the two arms as `path:line`. The `getLogger`/`shared/logger.js` half of the premise can stand as written; the level-per-arm half needs a real citation before it is load-bearing. |
| s2 | external-contract | MED | assisted | `npx tsx --test 'src/mcp/**/*.test.ts'` is the sweep command that exercises the new tests and `npm run build` is the build guard referenced by every smoke check. | The premise makes two claims; the evidence only partially bears on them.  1. Sweep command — supported, not contradicted. The `tsx --test` probe returned 50 matches confirming `npx tsx --test <glob>` is the repo's established test invocation, and it located MCP tests at the paths the premise's glob would cover: `src/mcp/build-step/__tests__/build-step.test.ts:13` (`Run: npx tsx --test src/mcp/build-step/__tests__/build-step.test.ts`) and `src/mcp/workflow-step/__tests__/amendments-e2e.test.ts:22`. Both sit under `src/mcp/`, so `src/mcp/**/*.test.ts` does resolve to them. Note the canonical repo-wide sweep the code itself emits is a different glob: `src/mcp/build-step/render.ts:29` → ``const TEST_CMD = `npx tsx --test 'src/**/__tests__/*.test.ts'`;`` — the premise's per-module glob is a narrowing of that, not a conflict.  2. Build guard — UNVERIFIABLE. The premise anchors on `package.json` and `npm run build`, but the `"build":` probe returned **0 matches**, and the only read of the anchor file returned a single line, `package.json:1 → FOUND: {`. Nothing in the gathered evidence shows a `build` script exists in `package.json`, nor any smoke check that references `npm run build`. The anchor is asserted but not grounded. (The 0-match result is not by itself proof of absence — the probe's scope is not shown, and every other match in the evidence set is under `src/`, so it may never have read `package.json` at all.)  The second claim — "the build guard referenced by every smoke check" — is a universal quantifier over smoke checks with zero supporting matches in the evidence. | Re-probe the `npm run build` half of the premise against `package.json` directly (read the `scripts` block, not a `"build":` grep whose scope excluded the anchor file), and grep the smoke checks for their actual build invocation. If `scripts.build` exists, tighten the premise to cite it by line; if it does not, the premise is HIGH and must name the real build entrypoint. Separately, consider whether the sweep should use the repo-canonical glob from `src/mcp/build-step/render.ts:29` rather than a bespoke per-module one. |
| s2/t6 | inventory | LOW | manual | Exactly three structurally identical envelope types exist and are the targets of the additive _meta field: WorkflowStepMcpEnvelope in src/mcp/workflow-step/types.ts, StepMcpEnvelope in src/mcp/analyze-step/types.ts, and BuildStepMcpEnvelope in src/mcp/build-step/types.ts. | Re-derived grep `/interface (WorkflowStepMcpEnvelope\|StepMcpEnvelope\|BuildStepMcpEnvelope)/` returns exactly 3 matches, one per named type, each at its anchored path: src/mcp/workflow-step/types.ts:185 `export interface WorkflowStepMcpEnvelope`, src/mcp/analyze-step/types.ts:146 `export interface StepMcpEnvelope`, src/mcp/build-step/types.ts:92 `export interface BuildStepMcpEnvelope`. The broader `/McpEnvelope/` sweep (9 matches) surfaces no fourth envelope type — the remaining 6 hits are import sites and handler return annotations within the same three families (analyze-step/handler.ts:24,56; build-step/handler.ts:17,23; workflow-step/handler.ts:22,31). So "exactly three" is neither an undercount nor an overcount, and each anchor resolves to a real declaration rather than a re-export or alias. Caveat: the premise's "structurally identical" qualifier is NOT covered by the gathered evidence — all three reads resolved to line 1 of each file (the license banner comment), not the interface bodies at :185 / :146 / :92, so field-level congruence was never probed. | none — the inventory (exactly three envelope types, at the three anchored paths) is verified sound. Optionally re-probe with reads at src/mcp/workflow-step/types.ts:185, src/mcp/analyze-step/types.ts:146, and src/mcp/build-step/types.ts:92 to substantiate the unprobed "structurally identical" qualifier before relying on a single shared shape (e.g. a common base interface or one patch applied verbatim to all three). |
| s2/t6 | closed-union | LOW | manual | None of the three envelope types currently declares a _meta field, so adding `readonly _meta?: LongRunningToolMeta \| undefined` is purely additive. | The deterministic re-run of the premise's probe searched the source tree for `_meta` and returned **0 matches**. Since the premise is an absence claim — "none of the three envelope types currently declares a `_meta` field" — a zero-hit grep across the tree is directly confirming evidence: if any of `src/mcp/workflow-step/types.ts`, `src/mcp/analyze-step/types.ts`, or `src/mcp/build-step/types.ts` declared `_meta`, the grep would have hit it. The single read anchor, `src/mcp/workflow-step/types.ts:1`, resolved successfully (returns the file's license header comment), confirming the anchor path exists and is readable. Nothing in the gathered evidence contradicts the "purely additive" conclusion — there is no pre-existing `_meta` declaration anywhere for the new `readonly _meta?: LongRunningToolMeta \| undefined` field to collide with or shadow. | none — verified sound. The 0-match grep for `_meta` confirms the field is absent across the tree, so adding it to the three envelope types is additive with no shadowing or redeclaration risk. (Minor, non-blocking: the evidence read only confirmed line 1 of one of the three anchor files, so the existence and shape of the envelope types themselves was not independently re-derived here — but that is not what this premise asserts, and the absence claim it does assert is fully supported.) |
| s2/t1 | citation | LOW | manual | src/daemon/workflow-rpc.ts declares onProgress at approximately line 75. | Direct read of `src/daemon/workflow-rpc.ts:75` returned FOUND: `readonly onProgress?:      ((f: WorkflowProgress) => void) \| undefined;` — an exact declaration of `onProgress` at the cited line, not merely a usage. The ripgrep sweep corroborates this: of the 22 `/onProgress/` matches, the only ones in `src/daemon/workflow-rpc.ts` are line 75 (the declaration) and lines 110, 112, 123, 131, 134, 151, 166, 184, 238 (all call sites of the form `opts.onProgress?.({ ... })` plus the wiring at 238 `onProgress: (f) => send({ id: 0, stream: 'progress', data: f })`). No other declaration of `onProgress` exists in that file, so the anchor resolves unambiguously to the intended entity. Other declarations found by the grep live in unrelated files (`src/cli/services/index.ts:70`, `src/cli/services/setup.ts:86`, `src/indexer/embedder.ts:288`, `src/workflow/review/review.ts:43`) and do not compete with the cited anchor. | none — verified sound. The citation `src/daemon/workflow-rpc.ts:75` is exact (not approximate), and the premise's hedge "at approximately line 75" is if anything weaker than the evidence supports. |
| s2/t1 | citation | LOW | manual | src/daemon/workflow-rpc.ts binds progress frames to the transport at approximately line 238 with the literal call send({ id: 0, stream: 'progress', data: f }). | Direct read of the cited anchor returned exactly the cited literal: `src/daemon/workflow-rpc.ts:238` → `onProgress: (f) => send({ id: 0, stream: 'progress', data: f }),`. The `stream: 'progress'` grep (21 matches) shows this is the sole progress-frame binding in `workflow-rpc.ts` — every other match lives in `src/daemon/analyze-rpc.ts:462,521`, `src/daemon/index.ts:1439`, tool builtins (`http`, `k8s`, `ssh`, `shell/exec-detached`, `test`, `cloud/aws/logs`), or `src/bin/__tests__/permission-hook-integration.test.ts`. The `id: 0` grep corroborates the transport block at `workflow-rpc.ts:213, 218, 238, 239, 248, 250`, confirming line 238 sits inside the `workflow.run` send-frame region alongside the sibling `stream: 'delta'` binding at line 239. Both the line number ("approximately line 238" — exact) and the literal call text are confirmed by ground truth. | none — verified sound |
| s2/t2 | inventory | LOW | manual | A repo-wide grep for progressToken, notifications/progress, and sendNotification returns zero hits, so the entire sc2 progress type surface is net-new with nothing to extend in place. | All three probes re-run against the source tree returned nothing: grep /progressToken/ → 0 matches; grep /notifications\\/progress/ → 0 matches; grep /sendNotification/ → 0 matches. This is exactly what the premise asserts — a repo-wide search for the three MCP-progress anchors yields zero hits, so no existing progress-notification type surface exists to extend in place, and the sc2 surface is net-new. | none — verified sound. (Note for the reader, not a defect: this is a negative/absence claim, so it is only as durable as the anchor list. If sc2 later introduces progress plumbing under a different name — e.g. a `progress` field on an existing frame type rather than the MCP `notifications/progress` spelling — the "nothing to extend in place" conclusion would need re-checking. Nothing in the gathered evidence contradicts it today.) |
| s2/t7 | ordering | LOW | manual | t7 and t11 are gated on s1's sc1 ProgressEvent reshape: until the daemon's stream:'progress' `data` payload is a sc1 ProgressEvent rather than WorkflowProgress, the reader has nothing conforming to consume. | Evidence confirms the premise's factual core. `src/daemon/workflow-rpc.ts:61` declares `export interface WorkflowProgress {`; line 75 types the callback as `readonly onProgress?: ((f: WorkflowProgress) => void) \| undefined;`; and the read-confirmed line 238 is `onProgress: (f) => send({ id: 0, stream: 'progress', data: f }),` — `f` is forwarded unwrapped, so the `data` payload on the wire is structurally a `WorkflowProgress`, exactly as the premise states. The 21 `stream:'progress'` matches show workflow-rpc.ts:238 is the sole workflow-path producer; all others are tool builtins emitting `{ message }` (http/k8s/ssh/shell/test/aws-logs), `analyze-rpc.ts:521` emitting `eventToProgressData(event)`, `index.ts:1439` emitting a model-pull frame, and three test fixtures emitting `{ verdict }`. The ordering constraint therefore has exactly one producer edge, matching the premise's framing. Caveat: no probe was run for the `ProgressEvent` anchor, so its absence from the tree is assumed, not demonstrated by the gathered evidence. | none — verified sound. Optionally add a `ProgressEvent` probe on a later pass to positively confirm the type does not yet exist, closing the one assumed half of the premise. |
| s2/t3 | citation | LOW | manual | makeSamplerFromMcpServer is defined in src/mcp/sampling-bridge.ts and is a reach-back-to-live-server closure precedent, NOT a ProgressSink call site as the LLD's callSites entry claims. | Symbol grep for /makeSamplerFromMcpServer/ returns exactly 3 matches, all accounted for: (1) definition at src/mcp/sampling-bridge.ts:53 `export function makeSamplerFromMcpServer(`, (2) import at src/mcp/server.ts:49 `import { makeSamplerFromMcpServer } from './sampling-bridge.js';`, (3) invocation at src/mcp/server.ts:576 `makeSamplerFromMcpServer(server.server),`. This confirms the premise's definition anchor verbatim (sampling-bridge.ts) and confirms the reach-back-to-live-server closure characterization — the call site passes the live `server.server` handle into the factory. The companion grep for /sampling-bridge/ (3 matches: the logger tag at sampling-bridge.ts:36, the test import at src/mcp/__tests__/sampling-bridge.test.ts:11, and the same server.ts:49 import) surfaces no additional consumers. Because the symbol grep is exhaustive, the negative claim holds: none of the 3 occurrences sits in a ProgressSink context, so this symbol is not a ProgressSink call site. Caveat: the read of src/mcp/sampling-bridge.ts:1 returned only the license header, so the function body was not directly observed — the closure semantics are inferred from the call-site signature at server.ts:576, not read. This does not affect either load-bearing claim. | none — verified sound. The premise's definition anchor (src/mcp/sampling-bridge.ts) and its "not a ProgressSink call site" claim are both confirmed by the exhaustive symbol grep. If the LLD's callSites inventory does list makeSamplerFromMcpServer as a ProgressSink call site, that inventory entry is the defect and should be removed there — but that is an edit to the LLD's callSites list, which was not handed to this review, so no artifact edit is proposed here. |
| s2/t4 | citation | LOW | manual | EmissionFault is referenced by the LLD but has no definition site anywhere in the source tree. | The deterministic probe `grep /EmissionFault/` returned 0 match(es) across the source tree. The premise is an absence claim ("no definition site anywhere in the source tree"), and an empty match set is exactly the evidence that confirms it — there is no `class`, `interface`, `type`, `enum`, or `const` declaration of `EmissionFault`, and no import or re-export of that symbol. Note the probe also returns zero *reference* sites in source, which is consistent with the premise's framing that the only mention is in the LLD prose (a document, not indexed by this source-tree probe) rather than in code. | none — verified sound. The LLD's reference to `EmissionFault` is dangling as claimed; no correction to the premise is warranted. |
| s2/t1 | citation | LOW | manual | runWorkflow at src/cli/command.ts:126 is the CLI dispatcher, not the workflow runner entry point, and must not be targeted by the wiring Tasks. | The gathered evidence confirms the premise on both halves. (1) The anchor resolves exactly: `read src/cli/command.ts:126 → FOUND: function runWorkflow(sub: string \| undefined, rest: readonly string[], svc: Services, ctx: CommandCtx): string[] {`. Its signature is CLI-dispatcher-shaped — it takes a subcommand string, a `rest` argv tail, a `Services` handle and a `CommandCtx`, and returns `string[]` (rendered output lines), not a workflow result. (2) Its sole call site is the CLI command switch: `src/cli/command.ts:55: case 'workflow': case 'wf': return runWorkflow(sub, rest, svc, ctx);` — dispatch, not execution. (3) The real workflow runner entry point is a distinct symbol in a distinct module: `src/daemon/workflow-rpc.ts:90: export async function runWorkflowServerSide(`, re-entered at `src/daemon/workflow-rpc.ts:236: const drive = (): Promise<RunWorkflowResult> => runWorkflowServerSide(intent, provider, {` and exercised end-to-end by `src/daemon/__tests__/workflow-rpc.test.ts` (lines 25, 72, 76, 94, 99, 105, 114). All 12 `runWorkflow` matches partition cleanly into the two CLI-dispatcher hits and ten `runWorkflowServerSide` hits — there is no third candidate that the wiring Tasks could be confusing for the runner. | none — verified sound. The premise's warning holds: wiring Tasks should target `runWorkflowServerSide` (src/daemon/workflow-rpc.ts:90), not `runWorkflow` (src/cli/command.ts:126). |
| s2/t7 | closed-union | LOW | assisted | s2 modifies no file under src/daemon/ — the send({ id: 0, stream: 'progress', data: f }) binding stays byte-identical and no new IpcStreamKind member is added. | Both load-bearing anchors resolve exactly as the premise states. (1) The `send` binding is byte-identical: the targeted read of `src/daemon/workflow-rpc.ts:238` returned `onProgress: (f) => send({ id: 0, stream: 'progress', data: f }),` — FOUND, matching the premise text character-for-character. (2) `IpcStreamKind` has exactly 2 occurrences, both in `src/shared/types.ts` (`:732` the union declaration, `:736` the `stream:` field usage), and `'progress'` is already a member of that union — so no new member is required for the s2 path. The 21 `stream: 'progress'` matches show the emitter is an established, widely-used pattern (analyze-rpc.ts:521, index.ts:1439, and 14 tool builtins), consistent with s2 reusing it rather than extending it. | none — verified sound. Note for precision only (non-blocking): `IpcStreamKind` is declared in `src/shared/types.ts:732`, not under `src/daemon/`, so the "no new IpcStreamKind member" clause is actually a constraint on `src/shared/`, not on the `src/daemon/` scope the premise names. This does not weaken the premise — the union already contains `'progress'` — but the two clauses guard two different directories. |

#### Proposed fixes

- **s2/t7** (assisted) — The anchor verifies, but the premise over-generalizes from 3 streaming-RPC sites to all progress frames, and 13 tool-builtin sites contradict it. Because the false universal is what makes the closure requirement look forced, the fix is a scope correction plus a decision about whether the closure conclusion still stands — a human should pick, and the artifact markdown was not supplied verbatim, so no mechanical find/replace is safe here.
  - option: Scope-only edit: replace 'Every progress frame emitted on the wire' with 'Every progress frame emitted by the workflow.run / analyze / model-pull streaming handlers', keeping the closure conclusion as an observation about the current code rather than a transport constraint.
  - option: Scope + counter-evidence edit: as above, and add a sentence recording that the tool-executor path sends `id: deps.requestId` on the same channel, so an id-carrying frame is an available alternative to closure.
  - option: Re-derive the conclusion: treat `id: 0` as an incidental convention and re-evaluate whether workflow.run should thread the real request id instead of relying on lexical closure — this would change the design, not just the premise wording.
  - option: Drop the premise as load-bearing: if nothing downstream actually depends on frames being uncorrelatable, remove the 'must' claim and keep only the factual observation about workflow-rpc.ts:238.

- **s2/t11** (manual) — The premise is wrong on every checkable dimension (count, path, payload shape) and its dormancy conclusion may be inverted — the only producer found emits line/token-shaped frames, not stage frames. There is no evidence-derived value to swap in, so any find/replace would be fabrication. Resolving this requires a design decision about which layer actually emits progress and whether the stage variant is the dormant one.
  - option: Re-anchor to the client layer: restate the premise as 'the single progress emission site is `src/cli/services/setup.ts:101`, emitting `{ model, line }`', then re-derive which variant (stage vs token) is actually dormant — the evidence suggests it is the stage variant, the opposite of what the premise asserts.
  - option: Treat daemon-side emission as not-yet-existing: rewrite the premise as a gap statement ('no daemon-side progress emission sites exist today') and move the eight-site inventory into the work to be built rather than the observed baseline.
  - option: Re-run discovery with corrected probes before restating: the anchors `onProgress` / `kind: 'stage'` may be the wrong search terms for how progress is actually modeled — locate the real progress frame type and its constructors first, then rebuild the inventory from that.
  - option: Drop the premise entirely and let the downstream step derive the emission inventory itself, if no step's acceptance criteria depend on a fixed site count.

- **s2/t1** (assisted) — The factual half of the correction is mechanical and fully evidence-derived: both functions have empty parameter lists per their declaration lines, and BuildInsrcMcpServerOpts has zero matches so it cannot be an existing type. But the artifact's *intent* behind naming that type is ambiguous from the evidence alone — it may be a hallucinated reference, or it may be a to-be-created type the LLD means to introduce. Those two readings imply different downstream work, so a human should pick before the premise is rewritten. Candidate edits below cover the two safe readings; neither invents a shape for the type.
  - edit: `buildInsrcMcpServer and runInsrcMcpStdio exist as real symbols in src/mcp/ but their parameter lists and the shape of BuildInsrcMcpServerOpts are unverified in the LLD ('UNVERIFIED SHAPE' placeholder).` → `buildInsrcMcpServer and runInsrcMcpStdio exist as real symbols in src/mcp/server.ts and both take NO parameters: `buildInsrcMcpServer(): McpServer` (server.ts:113, called with no args at server.ts:503) and `runInsrcMcpStdio(): Promise<void>` (server.ts:481, called with no args at src/bin/insrc-mcp.ts:45). BuildInsrcMcpServerOpts does not exist anywhere in the source tree (0 matches) — it is not an existing type whose shape is unverified.`
  - option: The type was hallucinated: rewrite the premise to state that both functions are parameterless (`buildInsrcMcpServer(): McpServer`, `runInsrcMcpStdio(): Promise<void>`) and remove `BuildInsrcMcpServerOpts` from the anchors entirely.
  - option: The type is intended as new: rewrite the premise to state that `buildInsrcMcpServer` currently takes no parameters, and that `BuildInsrcMcpServerOpts` is a NEW type this LLD must introduce along with a widened parameter list — making it a deliverable rather than a citation.
  - option: Defer: downgrade the premise from `kind: citation` to an open design question and resolve the options type in a follow-up, leaving the two verified parameterless signatures recorded as fact.

- **s2/t1** (assisted) — The evidence conclusively enumerates 11 existing test files in src/mcp/workflow-step/__tests__, contradicting the 'no test file to extend' premise. The factual correction (the directory is populated) is safe to state, but selecting WHICH file to extend is a scoping decision the grep cannot make: handler.test.ts and plan-e2e.test.ts are both plausible owners depending on whether the change is handler-level or workflow-level, and a new file may still be right if the behavior is genuinely a new axis. Hence assisted rather than auto — no artifactEdits are emitted, because every candidate replacement encodes a target choice the evidence does not determine.
  - option: Extend src/mcp/workflow-step/__tests__/handler.test.ts — correct if the change is at the workflow-step handler/dispatch level.
  - option: Extend src/mcp/workflow-step/__tests__/plan-e2e.test.ts — correct if the change is specific to the `plan` workflow's end-to-end behavior.
  - option: Keep a new test file, but amend the premise to 'no test file covers <specific behavior>' and cite the 11 enumerated files as reviewed-and-rejected rather than claiming none exist.
  - option: Split: add regression assertions to handler.test.ts and keep the new file for the end-to-end path, if the change spans both levels.

- **s2/t7** (assisted) — The count "exactly two files" is mechanically refuted by the evidence — src/mcp/server.ts:46 and :419 are non-test references. But the fix is not purely mechanical: the premise's substantive claim is about where `_meta` first becomes observable, and `server.ts:419` discarding `_extra` means the correct entry point depends on how `_meta` actually arrives (inside `rawArgs` vs. via the MCP extra argument). That distinction changes which file the plan must edit first, so a human should pick the wording rather than have the count silently patched.
  - edit: `The workflow-step wiring spans exactly two files: the request entry handleWorkflowStep in src/mcp/workflow-step/handler.ts where _meta first becomes observable, and the per-phase dispatch point handleStep in src/mcp/workflow-step/phases/step.ts.` → `The workflow-step wiring spans three files: the MCP tool registration in src/mcp/server.ts (import at :46, callback at :419, which currently forwards only rawArgs and discards _extra), the request entry handleWorkflowStep in src/mcp/workflow-step/handler.ts, and the per-phase dispatch point handleStep in src/mcp/workflow-step/phases/step.ts:30. Where _meta first becomes observable depends on whether it arrives inside rawArgs or via the discarded _extra parameter — this must be confirmed before scoping the edit.`
  - option: Treat server.ts as the real entry: rewrite the premise as three files, naming `src/mcp/server.ts:419` as the request entry where the MCP callback currently drops `_extra`, with handler.ts and phases/step.ts downstream. Choose this if `_meta` arrives via `_extra`.
  - option: Keep handler.ts as the `_meta` observation point but fix the count: three files, with server.ts listed as the registration/pass-through site. Choose this if `_meta` is already carried inside `rawArgs`.
  - option: Narrow the premise's scope instead of its count: drop the 'exactly two files' framing and state only the two files the plan intends to edit, explicitly noting server.ts as untouched pass-through.
  - option: Defer: add a verification step to the plan that reads `src/mcp/server.ts:410-425` and the `handleWorkflowStep` signature to determine the `_meta` path before fixing the premise.

- **s2/t1** (assisted) — The premise makes two claims — an exhaustive count ("exactly three") and a registration location ("inside buildInsrcMcpServer") — and the gathered evidence supports neither. The only buildInsrcMcpServer hits are its declaration and one call site with no body read, and the registration grep was capped inside the unrelated daemon capability registry. No safe auto-edit exists because the evidence supplies no verified replacement count or member list; inventing one would violate grounding. Both alternatives below need a human to pick after the scoped re-probe, since one weakens the premise and the other requires new evidence.
  - option: Re-probe first, then correct: read src/mcp/server.ts:113+ to enumerate the real registrations, and rewrite the premise's count and member list from that enumeration. Preferred — it resolves the premise rather than hedging it.
  - option: Weaken the premise to what the current evidence does support: 'The workflow-step, analyze-step, and build-step handlers each exist under src/mcp/ and dispatch on a `phase` field' — dropping both the exhaustive 'exactly three' count and the unverified buildInsrcMcpServer registration claim.
  - option: Split into two premises: one inventory premise over the step-tool handlers (verifiable from the handler.ts hits already gathered) and one separate registration premise anchored on src/mcp/server.ts, so the unverified half does not block the verified half.
  - option: Fix the anchor set only: replace the unmatched `src/mcp/workflow-step` anchor with the paths the evidence actually resolves (src/daemon/workflow-rpc.ts:10), and leave the count claim flagged as unverified pending the scoped re-probe.

- **s2/t1** (auto) — The premise records an unknown that the re-run probes now resolve definitively, and it resolves against the branch t8 hedges toward. The replacement is derived entirely from the gathered evidence: analyze-rpc.ts:521 and workflow-rpc.ts:238 emit the identical `{ id: 0, stream: 'progress', ... }` frame, and daemon/index.ts:1415 confirms the handler receives `send`. The edit converts the conditional into the verified statement and drops the now-dead narrowing clause, while flagging the payload-shape question the evidence genuinely does not answer.
  - edit: `src/daemon/analyze-rpc.ts exists and whether its analyze.run frames reach the wire through the same stream:'progress' binding as workflow-rpc.ts is unverified — t8's rollout narrows to workflow.run only if it does not.` → `src/daemon/analyze-rpc.ts exists and its analyze.run.start frames DO reach the wire through the same stream:'progress' binding as workflow-rpc.ts — analyze-rpc.ts:521 sends { id: 0, stream: 'progress', data: eventToProgressData(event) } against workflow-rpc.ts:238's { id: 0, stream: 'progress', data: f }, and daemon/index.ts:1415 registers the handler with a send argument. t8's rollout therefore does NOT narrow to workflow.run on transport-binding grounds. Open residual: the two data payload shapes (eventToProgressData(event) vs. the raw workflow frame) are not verified compatible for a shared consumer.`
  - option: Replace the unverified conditional with the verified finding and note the residual payload-shape question (recommended — matches the evidence exactly).
  - option: Replace with the verified finding only, and file the payload-shape compatibility check as a separate new premise rather than a trailing clause.
  - option: Keep the premise as a resolved-question record: mark it VERIFIED in place and add an explicit t8 directive that the rollout covers both analyze.run and workflow.run.

- **s2/t2** (manual) — No safe auto-edit is derivable: the evidence contains no matches inside the anchored directory, so neither the count (32) nor the casing claim nor the refutation of the LLD rationale can be corrected to a verified value. The truncation at the 50-match cap also means absence of `src/mcp/workflow-step` matches is an artifact of the probe's scope and ordering, not proof the directory has no classes — so it cannot be flipped to a negative claim either. A re-scoped probe is a prerequisite to any edit.
  - option: Re-run the class grep scoped to `src/mcp/workflow-step` with the cap removed, then amend the premise's count and casing claim to whatever that scoped evidence shows.
  - option: Narrow the premise to what a scoped probe can actually establish (a class count for the module) and drop the derived assertion that it refutes the LLD's 'class sample size 0' rationale, which needs a separate reading of the LLD's scope definition.
  - option: Drop the premise from the artifact if `src/mcp/workflow-step` turns out not to exist as a directory (e.g. the module is a single `workflow-step.ts` file), and re-anchor any surviving claim to the real path.

- **s2/t2** (auto) — Both anchor paths are non-existent; the evidence supplies the exact real paths via grep hits at src/mcp/analyze-step/state-store.ts:47 and src/mcp/workflow-step/questions-gate.ts:34. The replacements are verbatim from the gathered matches, not inferred. The 'sibling' wording is corrected in the same pass because the two files are not in a shared directory — but this edit is scoped to path/wording accuracy only and preserves the premise's verified kebab-case conclusion.
  - edit: `src/mcp/state-store.ts` → `src/mcp/analyze-step/state-store.ts`
  - edit: `src/mcp/questions-gate.ts` → `src/mcp/workflow-step/questions-gate.ts`
  - edit: `src/mcp/ uses kebab-case filenames with state-store.ts and questions-gate.ts as the attested sibling examples the new sink filename must follow.` → `src/mcp/ uses kebab-case filenames throughout; analyze-step/state-store.ts and workflow-step/questions-gate.ts are the attested examples the new sink filename must follow (they live in separate subdirectories, so they attest naming only, not placement).`
  - option: Apply both path corrections and the reworded premise (recommended — all three are evidence-derived).
  - option: Apply only the two path corrections, leaving the 'sibling' wording for a human to re-scope alongside the sink's directory decision.

- **s2/t3** (manual) — The evidence contains no declaration of ProgressEvent, StageProgressEvent, TokenProgressEvent, or any 'stage'/'token' discriminant — the sole hits are quoted fixture strings in the review framework's own test file. There is no evidence-derived value to substitute, so no artifactEdits can be emitted without inventing grounding. Choosing between 'this type is not yet written' and 'this premise points at the wrong name' is a design/scope judgement about the HLD, which requires a human.
  - option: Treat the union as prospective: restate s2/t3 in proposed voice ("the HLD introduces a ProgressEvent union with variants 'stage' and 'token'") and mark its probes as deferred until the implementing change lands, so the reviewer stops scoring it as an existing-code claim.
  - option: Treat the reference as stale: locate the progress/event type that actually exists in the daemon streaming path, re-anchor s2/t3 to its real declaration file:line, and re-derive the variant count and report() arms from that declaration rather than from the HLD prose.
  - option: Drop the exact-count assertion from s2/t3: keep the premise that report() branches per event kind with a default arm, but remove "exactly two variants", which is the part that cannot be verified and that silently gates acceptance on a number nothing in the tree supports.
  - option: Fix the probe set first and re-run before judging: exclude __tests__ fixture files from grep results for this premise, then re-gather — if the count is still 0 the premise is confirmed prospective rather than merely mis-probed.

- **s2/t3** (manual) — The evidence is uniformly negative — all four probes returned zero matches — which means there is no verified value to substitute in. Any auto-edit would require inventing field names or a file:line the evidence does not supply, which is exactly what must not happen here. The correct next move is a scoping/semantic decision by a human: determine whether these types exist under different names, exist outside the probed scope, or do not exist yet. Hence no artifactEdits.
  - option: Re-run the probes with no path scoping (and grep for the type names `StageProgressEvent` / `TokenProgressEvent` directly) to obtain real declaration-site reads; keep the premise as-is if the fields resolve.
  - option: If the types exist but the field names differ, correct the premise's field names from the re-derived declaration — a mechanical edit once real evidence exists.
  - option: If these types are introduced by this plan and not present in the current tree, reword the premise from an assertion about existing source into a specification of the contract to be built, and drop it from grounded-premise scoring.
  - option: If the mapping onto params.progress / params.total / params.message is the load-bearing part, add a separate probe targeting the mapping/adapter site so the mapping is verified independently of the field declarations.

- **s2/t10** (manual) — No safe mechanical edit exists. The evidence is silent on ProgressOperation (0 matches), so any rewrite of the union's membership would be invented rather than evidence-derived. Narrowing it to a one-member union on the strength of the single 'workflow.run' handler match would over-read the evidence: that match is an IPC registry key, not a union declaration, and its presence says nothing about whether sc1 intends analyze.run to follow. Choosing between a prospective-spec reading and a stale-anchor reading is a scoping decision.
  - option: Treat the premise as prospective: sc1 has not landed, so re-anchor it to the intended declaration site and re-verify after implementation, leaving the two-member claim unjudged for now.
  - option: Treat the anchor as stale/mis-resolved: locate where ProgressOperation is actually declared (possibly under a different name or in an untracked path such as src/workflow/review/ or the unstaged setup.ts files) and re-run the probe against the real symbol.
  - option: Narrow the premise to the one operation with a verified producer ('workflow.run', src/daemon/index.ts:1423) and record analyze.run as a planned-but-absent member, so the closed-union claim is not asserted over code that does not exist.
  - option: Drop the closed-union premise from the load-bearing set and re-derive it once the type is committed, since a union arity check has no ground truth to run against today.

- **s2/t1** (manual) — The evidence is a schema/test-fixture grep plus a one-character read of the wrong artifact (HLD, not LLD). It cannot confirm the count of nine, and it cannot identify which entries lack bundle grounding. Any find/replace on the artifact would have to invent a number or a member list the evidence does not support, so no artifactEdits are emitted. The remediation is a probe fix — re-target the LLD artifact and read the actual array — which is a decision about what to re-run, not a mechanical text correction.
  - option: Re-probe correctly and re-judge: point the read at the LLD artifact under `.insrc/artifacts/` and dump the full `body.errorPaths.invariantsToPreserve` array with each entry's `source`, then re-evaluate the premise against real data. Preferred — it converts this MED into a definite HIGH or LOW.
  - option: Narrow the premise to the part the evidence does support: keep the claim that enhancement-flavor invariants are *required* to cite an s1 analyze bundle (grounded at `src/workflow/runners/design-story/index.ts:336` and `:492`) and drop the unverified count and member enumeration until the artifact is read.
  - option: Leave the premise as written and record it as unverified pending artifact access, if the LLD artifact is not reachable in this review pass — but do not let it count as a confirmed inventory claim in the artifact's acceptance.

- **s2** (manual) — The premise is unverifiable from the gathered evidence, not demonstrably wrong. Every probe result is implementation code or test fixture data; the one artifact read returned a bare '{' and pointed at an HLD rather than the LLD s2 body. Correcting a count requires knowing the real count, and the evidence supplies none — so no find/replace can be derived without inventing a value. The remediation is to re-anchor and re-probe, which is a scoping decision rather than a mechanical text fix, hence manual with no artifactEdits.
  - option: Re-run the probes with anchors pointing at the LLD s2 artifact body (enumerate testStrategy.testLevels[*].subjects and acceptanceMapping[*].provingTests) and the plan body (testStrategyCoverage rows), then re-judge the twelve-item claim against real counts.
  - option: Weaken the premise to a structural, checkable assertion — e.g. 'every item in the LLD s2 testStrategy appears in the plan's Test-strategy coverage table with at least one covering Task' — dropping the hardcoded twelve so it survives artifact edits and is enforceable by checkTestStrategyCoverage (orchestrator.ts:1528).
  - option: Keep the cardinality claim but make it self-verifying: have the plan gate assert count(testStrategyCoverage) === count(derived LLD strategy items) and surface the two numbers on mismatch, rather than asserting a literal in prose.
  - option: Split the premise into two separately gradeable claims — (a) the s2 inventory size, (b) total coverage by Tasks — so a stale count does not mask an uncovered item, and vice versa.

- **s2** (manual) — The evidence is empty, so no text correction can be derived from it. Emitting an artifact edit would mean inventing a Task count or dependency set that the ground truth does not support. The blocking issue is that the probe's ID pattern does not match anything in the tree — deciding the correct grounding locus (artifact markdown vs. persisted Task records, and the real ID format) is a design/scoping call, not a mechanical fix.
  - option: Correct the probe's ID pattern (verify the real epic/story prefix and zero-padding used when Tasks are persisted) and re-run the inventory + dependency-closure check against non-empty output.
  - option: Re-point the premise's grounding at the artifact markdown itself if Tasks are never emitted into the source tree, and probe the Task headings and their 'Depends on' lines there.
  - option: Weaken the premise to drop the exact count ('the plan enumerates Tasks t1..tN, each Depends on entry naming only Tasks in that set') so it is verifiable without asserting a number the evidence cannot confirm.
  - option: Mark the premise as not-load-bearing and remove it if Task-set closure is not actually an acceptance condition for this artifact.

- **s2** (manual) — No safe mechanical edit is derivable: the evidence contains zero task identifiers and zero edges, so any correction to the edge list, the root set, or the anchors would be invented rather than evidence-derived. The premise may well be correct — it is simply untested by what the engine gathered. The remediation is to fix the probe, not the artifact text, which is a scoping decision for the plan author.
  - option: Re-probe against the plan artifact's task table body / `dependsOn` arrays (not the renderer), then re-judge the ordering premise on real edges — preferred, since it is the only path that actually tests the claim.
  - option: Narrow the premise's probe spec so it targets task-instance data by identifier (e.g. anchored on `t1`..`t11` occurrences) instead of the human-readable `Depends on` label that only appears in format strings.
  - option: Add t4 and t5 to the premise `anchors` so the anchor set covers every node named in the edge list, then re-probe.
  - option: Downgrade this premise to non-load-bearing if the task graph is validated elsewhere (e.g. by a topological-sort check in the plan artifact builder), and record that validator as the grounding instead.

- **s2/t5** (assisted) — The premise's material content — target directory and existence of a sibling exemplar — is confirmed by the successful read at src/mcp/__tests__/analyze-step-handler.test.ts:1, and the named test idiom is confirmed as the repo-wide norm by 100 truncated grep matches. What is unbacked is the attribution of specific internal conventions ('hand-written doubles, no external mocking library') to that one file, because both greps truncated before reaching src/mcp/ and only line 1 of the file was read. Since no evidence contradicts the premise, there is no evidence-derived replacement string to emit — an auto-edit here would have to invent a value. The correct remediation is either a cheap scoped re-probe or a wording change that keeps the claim within what the gathered evidence actually supports; both need a human call, hence assisted with options and no artifactEdits.
  - option: Re-run two file-scoped probes against src/mcp/__tests__/analyze-step-handler.test.ts (one for the node:test / node:assert/strict imports, one for mock/sinon/jest/vi.fn/t.mock) and re-verify the premise on the result — preferred, cheapest, and likely promotes the premise to LOW unchanged.
  - option: Weaken the attribution to what the repo-wide evidence already proves: keep 'src/mcp/__tests__/ is the target location and analyze-step-handler.test.ts is the sibling exemplar', but restate the conventions as the repo-wide standard ('node:test + node:assert/strict, per the prevailing convention across src/**/__tests__/') rather than as observed properties of that specific file.
  - option: Drop the unprobed clause only: remove 'hand-written doubles, no external mocking library' and retain the import-idiom claim, which the 100 grep matches support as the repo default.
  - option: Accept as-is and note it as a low-risk unverified descriptor — the premise steers file placement, and placement is fully confirmed by the successful read; a wrong doubles-style description would surface immediately at implementation time with no rework cost.

- **s2/t9** (assisted) — The anchor resolves to a real, readable file, so this is not a broken citation — but the probes only proved existence, never the frame-source claim the artifact leans on. A mechanical edit is unsafe because the evidence contains no verified alternative referent to substitute; picking one would be invention. The correct move is either a narrower re-probe or a weakening of the premise to exactly what was verified, which is a human call.
  - option: Re-probe the anchor with content patterns (`stream`, `'progress'`, `id: 0`) and, if they hit, restate the premise with the specific `path:line` of the stub block as grounding — this is the preferred path and likely resolves to LOW.
  - option: Weaken the premise to the verified claim only: 'src/daemon/__tests__/workflow-rpc.test.ts exists and covers workflow RPC' — drops the unverified frame-source assertion without asserting anything the evidence contradicts.
  - option: Keep the premise but demote it from load-bearing to advisory, so downstream acceptance does not depend on this file actually being the stubbing reference.
  - option: If the re-probe comes back empty, re-derive the correct anchor by grepping the tree for `stream:\s*'progress'` and repoint the citation at whichever test genuinely stubs that surface.

- **s2/t8** (assisted) — The evidence verifies the entry-handler half of the premise for both targets but is silent on the per-phase dispatch half — the only dispatch probe was a workflow-step-specific literal that could not have matched the other two modules. Correcting the text to claim only what the evidence supports is safe; asserting or denying the phases-directory shape is not. Re-scoping vs. re-probing is a human call, so no unattended edit is applied.
  - edit: `analyze-step and build-step have the same two-file shape as workflow-step — a request entry handler plus a per-phase dispatch point — so the t7 helper applies unchanged.` → `analyze-step and build-step each expose a single request entry handler (`handleAnalyzeStep` at src/mcp/analyze-step/handler.ts:56, `handleBuildStep` at src/mcp/build-step/handler.ts:23, wired at src/mcp/server.ts:300 and :471) matching workflow-step's entry shape. Whether they also share workflow-step's per-phase dispatch point (`./phases/step.js`, imported at src/mcp/workflow-step/handler.ts:18) is UNVERIFIED — confirm each module's dispatch shape before assuming the t7 helper applies unchanged.`
  - option: Re-probe first (recommended): list src/mcp/analyze-step/ and src/mcp/build-step/ and grep for `from './phases/` in both handlers, then restate s2/t8 with the actual finding. Cheapest path to turning this MED into a verified LOW.
  - option: Narrow the premise to the verified half: state that both modules expose a single request entry handler (analyze-step/handler.ts:56, build-step/handler.ts:23, both wired at server.ts:300 and :471) and mark the per-phase dispatch shape as an assumption t8 must confirm before applying the t7 helper.
  - option: Split s2/t8 into two tasks: one covering the entry-handler change (evidence-backed, ready to execute) and one covering the phase-dispatch change (blocked on confirming the dispatch shape in each module).
  - option: Leave the text as-is but attach an explicit risk note that 'the t7 helper applies unchanged' is unverified, and require t8 to fail loudly rather than adapt silently if either module's dispatch shape differs.

- **s2/t2** (assisted) — Every protocol-shape claim in the premise (method string, required/optional params, promise-returning sender) is a statement about an external SDK, and the re-run probes gathered zero evidence bearing on any of it — the greps for both the method string and any notification call came back empty, and the package.json read surfaced no version. The only thing the evidence supports is that the SDK is a dependency and that src/mcp/server.ts:36 uses the high-level McpServer facade. An auto-edit is unsafe because the correct replacement text is exactly what the evidence fails to supply; picking among the framings below is a human call.
  - option: Downgrade to a stated assumption: reword the premise to 'Per the @modelcontextprotocol/sdk progress-notification contract (version and exact surface to be confirmed against the installed SDK before implementation)...', keeping the design intent but removing the unearned verified-fact framing.
  - option: Add a blocking grounding sub-step to s2/t2 that pins the installed SDK version from package.json/lockfile and reads the progress-notification type plus the sender method off the McpServer instance at src/mcp/server.ts:36, then rewrites the premise from what that read actually returns.
  - option: Re-scope the premise to what this tree can support today: assert only that @modelcontextprotocol/sdk is a dependency (4 imports across src/mcp/sampling-bridge.ts and src/mcp/server.ts) and that no progress-notification call currently exists, making the task a greenfield addition rather than a claim about existing contract conformance.
  - option: Narrow the premise to the server surface question alone — resolve whether the high-level McpServer at src/mcp/server.ts:36 exposes a notification sender or whether the work must reach the low-level Server (type-imported at src/mcp/sampling-bridge.ts:27) — and defer the params-shape claims to implementation.

- **s2/t5** (assisted) — The premise is about package.json's dependency declarations, but the gathered evidence consists of a whole-tree grep (truncated, and matching only 'chain'/'keychain' substrings plus template strings) and a one-line read that stopped at the opening brace. Nothing observed confirms or refutes the claim, so no evidence-derived replacement text exists and no auto-edit is safe. The fix is to re-probe at the declaration site; a human should choose whether to re-ground, narrow the claim to what test bodies already support, or split the two clauses.
  - option: Re-run the probes correctly and keep the premise as-is if confirmed: read package.json's dependencies/devDependencies blocks and grep package.json specifically for sinon|jest|vitest|chai|proxyquire|testdouble|expect|should|nock|msw|ts-mockito|@sinonjs. If the blocks are clean, downgrade this finding to LOW with the real evidence attached.
  - option: Narrow the premise to the claim the evidence does support, e.g. 'test doubles in the existing suite are hand-written object literals and node:test mock.method (src/cli/__tests__/tui.test.ts:70-71, src/cli/__tests__/command.test.ts:47-48, src/daemon/db/__tests__/secrets.test.ts:7); t5 adds none.' This is grounded in observed test bodies rather than in an unread dependency list.
  - option: Split s2/t5 into two separately-anchored premises: one inventory premise anchored on package.json's dependency blocks ('declares no external assertion or mocking library'), and one scope premise anchored on t5's own diff ('t5 adds no such dependency'). The second clause currently has no probe of any kind and is silently riding on the first.
  - option: Accept the premise as an explicit project-convention assertion rather than a verified inventory fact — CLAUDE.md documents the sweep as `npx tsx --test 'src/**/__tests__/*.test.ts'`, implying node:test — and mark it as convention-sourced so the grounding engine stops trying to re-derive it from package.json.

- **s2/t6** (assisted) — The evidence neither confirms nor refutes the flags — the sole tsconfig read stopped at the opening brace and the flag greps appear not to have covered JSON config files. No text correction is derivable from what was handed over, so no artifactEdits are safe; the remediation is to widen the probe, then pick a rewording based on what it returns.
  - option: Re-probe: read tsconfig.json in full plus its `extends` chain, then re-anchor each of the three flags to a concrete `tsconfig.json:<line>` and keep the premise as written.
  - option: Weaken the anchor set: drop `noUncheckedIndexedAccess` (0 matches, and it is not what distinguishes the two `_meta` cases anyway) and rest the premise solely on `exactOptionalPropertyTypes`, verified against the config file.
  - option: Re-ground the premise in the codebase convention rather than the compiler: cite `src/workflow/review/extract.ts:161` as evidence that the omit-rather-than-`undefined` convention is followed in this module, and justify the s2/t6 split on that convention.
  - option: Re-scope s2/t6: if the flags cannot be verified as enabled, merge the `_meta?: LongRunningToolMeta | undefined` and `_meta: {}` cases into a single test and drop the strictness premise entirely.

- **s2/t3** (assisted) — The convention half (getLogger from src/shared/logger.js, no console) is verified by the evidence; the arm-level-mapping half is unverifiable from what was gathered, and the anchors point at the logger module rather than the emission site. No safe auto-edit exists because the correct file:line for the two arms is not in evidence — inventing one would be exactly the failure mode this check exists to catch.
  - option: Narrow the premise to what is verified: keep 'emission-fault logging uses getLogger from src/shared/logger.js, consistent with the project's no-console rule' and drop the warn/debug arm mapping until a real emission-site citation exists.
  - option: Keep the premise as written but re-anchor it to the emitter source file and re-run the probe with a grep scoped to that file, so the two arms resolve to concrete path:line.
  - option: Split into two premises: a LOW/verified convention premise (getLogger + no-console) and a separate, currently-unverified premise asserting the per-arm levels, so the unverified half cannot silently carry the artifact's acceptance.

- **s2** (assisted) — The sweep-command half is corroborated by the evidence and needs no edit. The build-guard half is unverifiable from what was gathered — the `"build":` probe returned 0 matches and `package.json` was read only to line 1 — so no auto-edit is safe: correcting it would require inventing a script name the evidence does not supply. The options below either re-scope the premise to what IS grounded or defer to a re-probe.
  - option: Split the premise: keep the sweep-command claim (grounded by src/mcp/build-step/__tests__/build-step.test.ts:13 and src/mcp/workflow-step/__tests__/amendments-e2e.test.ts:22) as its own s2, and move the `npm run build` claim to a separate premise with `package.json#scripts.build` as an explicit, re-probed anchor.
  - option: Weaken the universal quantifier: replace 'the build guard referenced by every smoke check' with 'the build guard' and add a concrete `file:line` anchor once package.json's scripts block has actually been read.
  - option: Re-probe first, then decide: read package.json's `scripts` object and grep the smoke checks for their build invocation; if `scripts.build` is absent this becomes a HIGH finding (the plan targets a non-existent producer) rather than an unverifiable one.
  - option: Align the sweep glob with the repo canon: change `src/mcp/**/*.test.ts` to the `src/**/__tests__/*.test.ts` form emitted at src/mcp/build-step/render.ts:29, accepting the wider blast radius in exchange for matching what the build-step renderer already tells users to run.

- **s2/t7** (manual) — The premise is confirmed by the evidence; no artifact edit is warranted. The only residual gap is that the `ProgressEvent` anchor was never probed, so the claim that the reader has 'nothing conforming to consume' rests on an unverified assumption about that type's non-existence. This is a probe-coverage gap in the verification pass, not a defect in the artifact text.
  - option: Accept as-is — the load-bearing claim (payload is WorkflowProgress at workflow-rpc.ts:238) is directly verified.
  - option: Add a `ProgressEvent` grep probe to this premise's anchor set so a future re-verification confirms the type's absence rather than assuming it.

- **s2/t7** (assisted) — The premise is verified against the evidence; the only improvement available is a precision cleanup, since the two clauses guard different directories (src/daemon/ for the send binding, src/shared/types.ts for the union). Needs a human OK because it is a wording change to a load-bearing constraint, not an evidence-derived factual correction.
  - option: Leave as-is — both clauses are individually verified and the conflation is harmless.
  - option: Split the scope explicitly: 's2 modifies no file under src/daemon/ (the send({ id: 0, stream: 'progress', data: f }) binding at src/daemon/workflow-rpc.ts:238 stays byte-identical) and no file under src/shared/ (no new IpcStreamKind member is added to src/shared/types.ts:732).'
  - option: Add the resolved anchor line numbers only, keeping the current sentence structure: cite src/daemon/workflow-rpc.ts:238 and src/shared/types.ts:732 inline so a future re-check is exact.