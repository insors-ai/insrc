// insrc JetBrains IDE plugin — a NEW Gradle/Kotlin subproject, deliberately
// distinct from the ESM/TS backend (Epic constraint k6). It is its own Gradle
// build (its own settings.gradle.kts root) so the two toolchains never entangle.
rootProject.name = "insrc-jetbrains"
