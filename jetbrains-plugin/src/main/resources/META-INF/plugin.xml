<idea-plugin>
    <id>ai.insors.insrc</id>
    <name>insrc</name>
    <vendor url="https://github.com/insors-ai/insrc">insors-ai</vendor>

    <description><![CDATA[
        <p><b>insrc</b> brings the insrc code-knowledge system into the JetBrains
        IDE family &mdash; IntelliJ IDEA, PyCharm, GoLand, and WebStorm &mdash; from
        a single install. It is a <b>thin orchestrator that owns no reasoning of its
        own</b>: it wires insrc's grounded-analyze and tracked-workflow capabilities
        into the IDE's own AI assistant and manages the backing daemon, so your
        assistant answers from a citation-grounded model of <i>your</i> codebase
        rather than guessing.</p>

        <h3>What is insrc?</h3>
        <p>insrc is a <b>local-first code-knowledge daemon</b>. It parses your repos
        with tree-sitter into a typed entity/relation <b>graph</b> (backed by LMDB)
        and a <b>vector store</b> (LanceDB) for semantic search, then serves two
        things to your AI assistant over a private MCP server:</p>
        <ul>
            <li><b>Grounded analyze</b> &mdash; every answer about your code is built
                from real graph queries and embedded search, and each claim is
                <i>cited to a specific file and symbol</i>. No raw file dumps, no
                hallucinated paths &mdash; the assistant reasons over a verified,
                dependency-scoped model of the codebase.</li>
            <li><b>A tracked workflow</b> &mdash; a human-in-the-loop authoring
                pipeline that takes one request from
                <code>triage &rarr; design &rarr; review &rarr; plan &rarr; build &rarr; code-review</code>,
                producing an approved, cited artifact at every stage and refusing to
                advance until you approve the one before it. Every change, large or
                small, is tracked; a defect fix routes through a first-class
                <i>bugfix</i> path that captures reproduction and root cause and can
                open, link, and close a GitHub issue for you.</li>
        </ul>
        <p>All of this runs on your machine and reasons through your assistant's
        existing <code>claude</code> / <code>codex</code> CLI sessions &mdash; there
        are no API keys and no direct cloud calls from insrc itself.</p>

        <h3>What the plugin does</h3>
        <ul>
            <li><b>One integration, four IDEs.</b> A single artifact activates
                identically in IntelliJ IDEA, PyCharm, GoLand, and WebStorm &mdash; it
                depends only on the shared platform module, so there is nothing
                IDE-specific to install or configure per product.</li>
            <li><b>Wires your AI host automatically.</b> On project open it detects
                your agentic assistant (JetBrains AI Assistant or Junie) and registers
                the insrc MCP server into that host's own configuration, plus the
                tracked-workflow steering into its rules file &mdash; so the assistant
                immediately knows to ground its answers and route features through
                insrc.</li>
            <li><b>One-click project enablement.</b> An "Enable insrc for this
                project" prompt registers the open project with the insrc daemon;
                every capability call is scoped to that project's own path, so a
                multi-project window keeps each repo's knowledge separate.</li>
            <li><b>Manages the backing daemon for you.</b> It ensures the daemon is
                present and up to date, and provisions a private, pinned Node runtime
                when the system one is missing or unsuitable &mdash; first-time setup
                is a single explicit consent, and later updates are silent and
                automatic.</li>
            <li><b>Grounded analyze + tracked workflow, in your assistant.</b> Once
                wired, your existing IDE assistant explores the code through the
                citation-grounded graph and can drive the full tracked design &rarr;
                review &rarr; plan &rarr; build workflow without leaving the IDE.</li>
            <li><b>Clean and reversible.</b> Every write into a host config or rules
                file is marker-delimited and preserves your own surrounding content.
                Uninstalling the plugin removes exactly those blocks and nothing else;
                a mere disable leaves them intact so re-enabling is instant.</li>
        </ul>

        <h3>How it works</h3>
        <p>The plugin holds no model logic of its own. It is a configuration
        orchestrator: it detects the host, writes the MCP + steering registration,
        registers the project with the daemon, and keeps the daemon healthy. All
        reasoning happens in your assistant, grounded by the daemon &mdash; the plugin
        simply makes the connection and stays out of the way.</p>

        <h3>Privacy</h3>
        <p>The plugin opens no cloud path and stores no credentials. The graph and
        vectors live on your disk; embeddings are computed locally; and all LLM
        access goes through your assistant's existing CLI/OAuth sessions. Nothing
        about your code leaves your machine because of this plugin.</p>

        <h3>Requirements</h3>
        <ul>
            <li>A JetBrains AI host (AI Assistant or Junie) &mdash; without one the
                plugin stays dormant and re-checks on later project opens, so it is
                safe to install ahead of time.</li>
            <li>Node (used by the daemon); the plugin provisions a private runtime
                automatically if the system Node is missing or unsuitable.</li>
            <li>The insrc daemon, which the plugin installs and keeps current on first
                use, with your consent.</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.2.10</h3>
        <ul>
            <li><b>Vertical scrolling fix.</b> The Settings page is now itself a scroll
                pane, so the IDE bounds it to the window and its vertical scrollbar
                reliably reaches all content &mdash; expanding a section no longer
                clips rows or makes the scrollbar flash and vanish.</li>
        </ul>
        <h3>0.2.8</h3>
        <ul>
            <li><b>Fixed vertical scrolling on the Settings page.</b> The page stays
                bounded to the window and its vertical scrollbar reaches all content;
                the earlier horizontal-growth glitch is gone (the page never scrolls
                sideways &mdash; it fills the width).</li>
        </ul>
        <h3>0.2.7</h3>
        <ul>
            <li><b>Compatibility:</b> removed all internal/deprecated platform-API
                usage flagged by the Plugin Verifier on newer IDEs &mdash; host
                detection uses the stable <code>isPluginInstalled</code>, and the
                Settings apply path no longer calls a deprecated exception accessor.
                The build now verifies against recent IDE releases so this class of
                drift is caught at build time.</li>
        </ul>
        <h3>0.2.6</h3>
        <ul>
            <li><b>Fixed: the Settings page clipped its content on expand.</b> Expanding
                a section grew the whole page to fit and pushed the scrollbar out, so
                content past the fold became unreachable. The page now stays bounded to
                the window and its scrollbar scrolls the overflow.</li>
        </ul>
        <h3>0.2.5</h3>
        <ul>
            <li><b>Compatibility:</b> the AI-host detection no longer calls an
                internal platform API (<code>PluginManager.findEnabledPlugin</code>,
                which newer IDEs mark internal); it now uses the public
                <code>getLoadedPlugins()</code> with the same installed-and-enabled
                semantics, keeping the plugin clean under the Plugin Verifier.</li>
        </ul>
        <h3>0.2.4</h3>
        <ul>
            <li><b>Fixed: per-repository overrides failed to load</b> ("class
                com.google.gson.JsonArray cannot be cast to&hellip;JsonObject"). The
                daemon's <code>repo.list</code> returns a bare JSON array; the transport
                now reads an array result correctly instead of crashing, so the
                per-repo override editor and its add-repository picker populate again.</li>
            <li>This also clears a brief scrollbar flicker that the long error message
                caused on the Settings page.</li>
        </ul>
        <h3>0.2.3</h3>
        <ul>
            <li><b>Settings page is now collapsible sections.</b>
                <b>Settings &rsaquo; Tools &rsaquo; insrc</b> is a single scrolling page
                of <b>expand/collapse</b> category sections &mdash; click a
                <b>&#9662;/&#9656;</b> header to open a category into its editable
                <b>key / value / default</b> table. <b>General</b> is expanded on load,
                the rest collapsed.</li>
            <li>The whole page scrolls as one, each table sized to its content, and the
                page fills the Settings window rather than a fixed size.</li>
            <li>The per-role and per-repository override editors are their own
                collapsible sections on the same page.</li>
        </ul>
        <h3>0.2.2</h3>
        <ul>
            <li><b>Reworked Settings page.</b> <b>Settings &rsaquo; Tools &rsaquo; insrc</b>
                is now an expandable category <b>tree</b> on the left with a
                <b>key / value / default</b> table on the right &mdash; every setting's
                current value and default are shown at a glance, and the page scrolls
                properly when there is a lot of content.</li>
            <li>Edit a value inline in the table (chooser / toggle / text by type) with
                a per-row reset-to-default; the <b>General</b> category is open by
                default.</li>
            <li>The per-role and per-repository override editors are now their own
                nodes in the same tree.</li>
        </ul>
        <h3>0.2.1</h3>
        <ul>
            <li><b>New: a native insrc Settings page.</b> Under
                <b>Settings &rsaquo; Tools &rsaquo; insrc</b> you can now see and edit
                every daemon setting from the IDE &mdash; grouped into collapsible
                sections and rendered entirely from the daemon's own description, so
                the plugin never forks or hardcodes the schema.</li>
            <li><b>Edit global settings</b> with type-appropriate controls (choosers,
                toggles, number and text fields), reset any setting to its default, and
                Apply &mdash; writes go straight to the daemon's config.</li>
            <li><b>Per-role overrides:</b> pick the model tier for each reasoning role,
                or fall back to the default routing.</li>
            <li><b>Per-repository overrides:</b> add an override for a registered
                repository and tune its full per-repo tiering &mdash; the accuracy
                floor, per-role tiers, and per-tier runner/model &mdash; without
                touching the global defaults; remove it to return the repo to global.</li>
        </ul>
        <h3>0.2.0</h3>
        <ul>
            <li><b>New: the in-IDE artifact review panel.</b> An "insrc Review" tool
                window lists the workflow artifacts awaiting your approval, and you can
                now handle each one in place without leaving the IDE:</li>
            <li><b>Read</b> an artifact's fully rendered content (design, plan, and
                review documents) in the panel.</li>
            <li><b>Annotate</b> it with PR-style inline comments anchored to the exact
                section or passage you're commenting on.</li>
            <li><b>Submit</b> your comments back as tracked open-question resolutions,
                so your feedback lands on the artifact itself.</li>
            <li><b>Approve</b> a pending artifact directly from the panel &mdash; with a
                clear block notice when a review verdict withholds approval, and an
                explicit "Approve anyway" override when you choose to proceed.</li>
            <li>Expanded the Marketplace listing with a fuller description of what
                insrc is (local-first code-knowledge daemon: grounded analyze + tracked
                workflow) and how the plugin wires it into your IDE assistant.</li>
        </ul>
        <h3>0.1.1</h3>
        <ul>
            <li><b>Compatibility:</b> removed the upper IDE-build bound &mdash; the
                plugin now loads on current and future IDE releases (2024.2 and
                later), since it depends only on the stable platform module.</li>
        </ul>
        <h3>0.1.0</h3>
        <ul>
            <li>Initial release.</li>
            <li>Single-artifact activation across IntelliJ IDEA, PyCharm, GoLand, and
                WebStorm.</li>
            <li>AI-host detection (AI Assistant / Junie) with automatic MCP-server and
                tracked-workflow steering registration.</li>
            <li>One-click project registration against the insrc daemon.</li>
            <li>Daemon lifecycle management: presence/staleness check, consented
                first-time setup, tiered Node runtime.</li>
            <li>Clean, reversible uninstall of all host-file writes.</li>
        </ul>
    ]]></change-notes>

    <!--
      Depend ONLY on the common platform module (Story S001 / lc1, ac3): this is
      what makes the SINGLE artifact load and activate in IntelliJ IDEA, PyCharm,
      GoLand, and WebStorm alike. Do NOT add an IDE-specific module dependency
      (e.g. com.intellij.modules.java / .python / .go) — that would restrict the
      artifact to one IDE and break the single-integration guarantee.
    -->
    <depends>com.intellij.modules.platform</depends>

    <!--
      sc1 project-open lifecycle (Story S001 / t2): a ProjectActivity that fires
      onProjectOpened once per opened project, off the UI thread.
    -->
    <extensions defaultExtensionNs="com.intellij">
        <postStartupActivity
            implementation="ai.insors.insrc.jetbrains.platform.InsrcProjectOpenActivity"/>
        <!--
          Notification group for insrc's user-visible outcomes (Story S003 / t7):
          the one-click daemon setup offer and setup/update failure reports.
        -->
        <notificationGroup id="insrc" displayType="BALLOON"/>

        <!--
          Review tool window (Epic ide-artifact-review-panel / Story S001 / t4):
          the pending-approval artifact list. Registered on the shared platform
          module only, so the single artifact hosts it in all four IDEs. anchor
          right keeps it out of the way of the project/structure views.
        -->
        <toolWindow
            id="insrc Review"
            anchor="right"
            secondary="true"
            factoryClass="ai.insors.insrc.jetbrains.review.ReviewToolWindowFactory"/>

        <!--
          Settings page (Epic expose-all-daemon-config-settings-via / Story S002):
          a native Settings ▸ Tools ▸ insrc page that renders the daemon's
          self-describing config.catalog read-only, grouped + collapsible. An
          application-level Configurable (settings are the daemon's global
          config.json, not per-project). Editing + overrides arrive in S003–S005.
        -->
        <applicationConfigurable
            parentId="tools"
            id="ai.insors.insrc.settings"
            displayName="insrc"
            instance="ai.insors.insrc.jetbrains.settings.InsrcSettingsConfigurable"/>
    </extensions>

</idea-plugin>
